# P0 traceability

Status: `PASS` for the engineering probe phase at HEAD `ce4a71c`; P1 is `READY` and has not started. This closure records probe evidence and explicit hand-offs. It does not claim production feature completion, release signing, or the later-stage security and performance matrices.

| Task | Implementation | Automated evidence | Manual / later-stage evidence |
| --- | --- | --- | --- |
| P0-01 | workspace/project, app, probe, package, configs, ad-hoc entitlements | `script/quality.sh` reached successful Debug, Release and P0 probe builds at `ce4a71c` | none specific to the skeleton |
| P0-02 | strict format, package tests, coverage artifact, quality script, CI | strict `swift-format lint` exit 0; SwiftPM focused suite exit 0 with 21 tests, line coverage 88.84% and region coverage 79.53%; `script/quality.sh` then stopped at XCUITest because the probe app did not load accessibility; the ordinary app-window UI test passed | remote/full release CI belongs to the version gate; the accessibility failure is retained as system evidence, not hidden by product code |
| P0-03 | `SynoraDomain` contracts, UUID/revision/hash types | included in the 21-test SwiftPM suite at `ce4a71c` | larger property sequences belong to the owning implementation stage |
| P0-04 | TextKit 2 bridge and Unicode fixture | P0 probe Debug build and ordinary application-window XCUITest passed; the probe editor/action test was blocked twice by `tech.atlax.SynoraWiki.P0Probes` not loading accessibility | real IME, attachment/undo, VoiceOver and 100k-character performance move to P2 |
| P0-05 | records+blocks+assets projections, snapshot recovery, shared replay state machine | prior `script/p0.sh store` heavy evidence remains recorded at commit `8313181`; focused store/replay tests pass in the current 21-test suite | schema migration and product-scale recovery belong to P3/P9; the heavy suite was intentionally not rerun in this closure |
| P0-06 | no code or target added | negative scan at `ce4a71c` found 0 product-code matches for CloudKit, sync, Companion, MusicKit and WeatherKit markers | synchronization remains DEFERRED; rerun the scan at later phase exits |
| P0-07 | epoch-interruption deadline + cancel in shim, service policy, capability host import and XPC fixture | `script/p0.sh skill` exit 0 with 10/10 focused tests at `ce4a71c`; earlier P0 quality evidence covers the XPC fixture and crash/reconnect drill, while the current quality invocation stopped before that phase at XCUITest | full file/network broker and malicious matrix move to P6; current system-level UI blocker is recorded, not bypassed |
| P0-08 | deterministic smoke/full generator with manifest schema v2 and media mapping | `script/p0.sh benchmark` exit 0 with deterministic/resume and media-fixture checks; full profile shape remains 10k records/100k blocks/20 GiB logical budget without regenerating the heavy corpus | physical 20 GiB generation and product-scale media performance move to P3/P9 |
| P0-09 | threat model, classifications, sentinel policy | threat controls and historical P0 sentinel evidence are recorded; the current quality retry did not reach sentinel scanning because XCUITest stopped earlier | release-wide dependency/log review moves to P9 |
| P0-10 | ADR-001…006 and feasibility evidence | ADR statuses and Harness state were synchronized after the focused checks; manifest valid and 98 references remain unbroken | later production decisions must retain the explicit P2/P3/P6/P9 hand-offs |

The table separates P0 feasibility evidence from later production acceptance. P0 closes after its focused checks, accurate system-level UI evidence and applicable ADR/status alignment; P2/P3/P6/P9 retain the transferred product, scale, security and release requirements.
