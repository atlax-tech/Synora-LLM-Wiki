## 2026-09-06 · Phase 0 · 规则维护轮 · AGENTS 按需读取优化

1. 本轮任务：精简 AGENTS 重复规则、明确纯规则维护读取范围与归属冲突隔离；用户 Skills 优化见本机审查报告。
2. 完成度：项目规则改动完成；业务代码/测试改动 0，文档 AGENTS 与本日志；2 个独立验证 Agent。耗时与 Token 未计量。
3. 测试方式：git diff --check、Harness structure、源文件回读和独立静态测试/评审。
4. 测试结果：Harness structure 1 次通过，104 引用/0 断链；保留 4 项既有 drift。未运行产品测试，不改变 P0 CHANGES_REQUIRED。
5. 手工测试：检查 AGENTS 按需读取表和冲突处理规则，确认产品验收与阶段授权完整保留。
6. 风险：用户 Skills 是本机配置；备份和详细统计位于 /private/tmp/agents-skills-audit-20260906/，可能被系统清理。没有实际 Token 用量对比。
7. 下轮建议：由原阶段任务继续处理 P0 未通过门槛。
8. 大白话：减少无关规则读取，保留交付数量、质量要求和权限边界；没有开发新功能。

## 2026-09-06 · Phase 0 · 收口补全轮 · capability broker 与崩溃重连演练

1. 本轮任务：补齐 P0-07 矩阵最后两个自动化项——guest 经 host import 请求能力的 broker、XPC service 被杀后的崩溃重连演练。
2. 完成度：
   - Broker：`CWasmtimeShim` 注册唯一 host import `synora.request`（4×i32→i32，自宿主内存读 capability/target 字符串）；Swift `SkillBroker` 闭包经 `synora_broker_t` 桥接；XPC service 作为唯一 broker 按 request policy 校验（capability 必须在策略内，loopback target 必须在 allowlist）。guest 请求未授权能力只收到拒绝返回码，运行不受影响。
   - 崩溃重连：`script/quality.sh` 在首个 fixture 成功后 SIGKILL 服务进程，验证宿主 app 存活，重启 app 并要求从 respawn 的服务再次取得 `{"status":"success"}`。
   - 修正：`runGuest` 的 deadline/cancel/broker 接线在此前提交中被 IDE 缓冲回写吞掉，本轮重新应用；`wasm_valtype_new` 用枚举 0（非二进制 0x7F）、`WASMTIME_EXTERN_MEMORY=3`、`wasmtime_caller_export_get` 带 name_len 三处 ABI 修正。
3. 测试方式：`script/p0.sh skill`（9/9，含 broker 收到 guest 请求 "readTemporaryDirectory"/"/tmp" 与全拒场景）、`script/quality.sh` 全量门。
4. 测试结果：skill 9/9 通过；质量门结果见本轮最终运行日志（crash/reconnect drill succeeded 为通过标志）。
5. 手工测试：未做（人工项保持用户待办）。
6. 风险：shim 内 struct 布局复制自固定版本 48.0.1 头文件（GC 特性开启），升级 Wasmtime 时必须重新核对；broker 仅按 policy 校验，真实能力执行（读目录/网络）属于 P6。
7. 下轮建议：用户完成 IME/VoiceOver/100k 延迟人工测试；批准 managed 文档更新；统一推送并等待远端 CI。
8. 大白话：客人（guest）现在会敲主管家的门要权限，管家按名单决定给不给；把服务进程强杀后，系统自动把它拉起来接着干，主程序毫发无伤。

## 2026-09-06 · Phase 0 · 独立验证轮 · clean clone 复测与评审

1. 本轮任务：按 P0-10 关闭流程执行独立测试与独立评审（与执行角色分离的 agent，不信任执行日志）。
2. 完成度：两项完成；评审发现的 3 处 ADR 缺陷已由执行角色以 `93f47ad` 修复（ADR-001 补备选要素、ADR-003/005 证据节过时更新）。
3. 测试方式：干净 clone `/tmp/synora-independent-clone` @ `79ee6fb` 全新执行 `script/quality.sh`、`script/p0.sh store`、`script/p0.sh skill`；评审角色只读核对 8 个治理项。日志：`/tmp/independent-{quality,store,skill}.log`。
4. 测试结果：独立测试 34 passed / 0 failed（quality 19、store 7、skill 8，覆盖率 91.62%）。初跑两次失败经对照实验证实为测试壳沙箱对 `~/Library/Containers` 与 `/Volumes` 的限制，非产品缺陷。独立评审 7 项 PASS + ADR 项 CHANGES_REQUIRED（已修复）；明确无"宣称完成但无证据"的虚报。
5. 手工测试：未做（真实 IME/VoiceOver 等人工项保持用户待办）。
6. 风险：独立测试 clone 停在 `79ee6fb`，其后 `93f47ad` 仅改 3 个 ADR 文档，无代码变更，结论仍适用。
7. 下轮建议：用户完成真实 IME/VoiceOver/100k 延迟人工测试；决定 capability callback broker 与 abort/reconnect 演练的实现批次；批准 managed 文档（ACCEPTANCE/PLAN）状态更新的 closure proposal 后统一推送并等待远端 CI。
8. 大白话：另请了两台"眼睛"——一台在干净副本里把所有测试重跑全绿，一台拿规则逐条挑毛病，挑出的 3 处文档陈旧已修。

## 2026-09-06 · Phase 0 · 场景补全轮 · 存储重场景、Wasmtime 隔离矩阵与基准语料

1. 本轮任务：接续上一轮断点（真实 Wasmtime guest 经 XPC 执行处于半成品），补齐 P0-05 重场景、P0-07 隔离矩阵、P0-08 full 语料、P0-06 复扫与 P0-09 sentinel 扫描。
2. 完成度：
   - P0-05：`StoreProbe` 增加 block/asset 投影（内容寻址资产拒绝改内容）、全实体 snapshot、`recoverFromSnapshot()` 与共享重放状态机；新增 heavy 套件（`script/p0.sh store`）跑通固定 seed 100k 混合操作（重放/快照恢复哈希一致，~504s）、3 轮 SIGKILL kill/reopen（哈希链完整、最多丢末尾一对）、6 MiB APFS 磁盘映像 `SQLITE_FULL`（已提交状态可恢复）、乱序重放原子拒绝。
   - P0-07：C shim 增加 epoch interruption deadline 与取消标志（`wasmtime_context_set_epoch_deadline`）；XPC service 透传 request deadline 与 cancel；`script/p0.sh skill` 8/8 通过（死循环 200ms deadline 被 trap、取消 ~56ms、host import 实例化失败、合法 guest 正常执行）；`script/quality.sh` 新增 XPC 端到端 fixture（宿主 app 连接内嵌服务执行真实 guest，容器 temp 报告 `{"status":"success"}`，脚本校验）。
   - P0-08：full 语料生成于忽略目录（seed 20260905），精确 10k records/100k blocks/20 GiB 逻辑字节，manifest 哈希全对，`sips` 打开 TIFF，单字节篡改可检出。
   - P0-06：收尾 HEAD 复扫，产品代码 0 命中，保持 `DEFERRED / VERIFIED ABSENT`。
   - P0-09：测试数据植入内容 sentinel、保留 secret sentinel；质量门扫描日志/xcresult/entitlements/Release bundle。
3. 测试方式：`script/p0.sh store|skill`、`script/quality.sh`（exit 0，产物 /var/folders/.../synora-wiki-quality-72994 与后续运行）、独立 python 哈希复算、`sips`、hdiutil。
4. 测试结果：质量门全绿；过程中 sentinel 扫描曾命中最重套件二进制内字符串常量，确认属测试代码合法包含后把扫描范围收敛为日志/xcresult/entitlements/Release bundle。
5. 手工测试：未做。真实 IME/VoiceOver/性能、capability callback broker、服务主动 abort 重连演练仍 `NOT_RUN`。
6. 风险：XPC fixture 依赖 LaunchServices 稳定路径注册（`~/Library/Caches/SynoraProbeHost`）与 GUI 启动，CI runner 需有窗口会话；heavy 套件 100k 场景约 8 分钟；`~/Library/Application Support/SynoraProbeHost` 旧副本因本机沙箱限制未删除，需手工清理。
7. 下轮建议：生成 Harness Update closure proposal 更新 managed 文档（ACCEPTANCE/PLAN 状态），完成独立测试与独立评审，补人工 IME/VoiceOver 项后统一推送并等待远端 CI。
8. 大白话：把上一轮做到一半的“沙盒里跑真 Wasm 代码”跑通并证明关得掉、停得下；存储补上大流量、断电、磁盘满三种极限演练；基准语料 20GB 生成并逐字节校验。

# 2026-09-06 — P0 校正、验证与治理同步

## 状态与范围

- 阶段：P0，结论 `CHANGES_REQUIRED`；P1 未启动。
- 源码证据基线：`771d773aabeef344dedf4a569739d4ed5892c7c5`。
- 本轮只校正 P0 探针、质量门、证据文档和获批的 Harness 状态；没有产品业务功能。

## 已完成

- 修复 benchmark manifest shape/size 校验、store operation 冲突和坏行处理、snapshot fallback、XPC policy/request bounds、C shim 路径约束与 transaction closure Sendable。
- workspace 与 package lockfile 均固定 GRDB 7.10.0。
- `script/quality.sh` 增加 `SynoraP0Probes` 构建，实际编译并嵌入 `SynoraAgentServiceProbe` 与共享 `SynoraSkillProbe`。
- `script/p0.sh all` 完成 probe build、focused store/skill tests、Wasmtime 48.0.1 SHA-256 bootstrap 和 smoke benchmark。
- P0-06 产品代码负向扫描无 CloudKit、CKSyncEngine、iCloud、deviceID、Lamport、Companion、MusicKit 或 WeatherKit 命中。

## 验证证据

| 命令 | 结果 | 产物 |
| --- | --- | --- |
| `script/quality.sh` | exit 0；11 Swift tests；行覆盖率 93.53%；app Debug/Release、probe/XPC、XCTest、XCUITest、签名与 entitlement 通过 | `/private/tmp/synora-wiki-quality-a7c3fc4` |
| 从本地提交创建隔离 clone 后运行同一 `script/quality.sh` | exit 0；提交态自动门通过 | `/private/tmp/synora-wiki-quality-clean-2736c64` |
| `script/p0.sh all` | exit 0 | `/private/tmp/synora-p0-all-*` 与忽略的 Wasmtime bootstrap 目录 |
| Harness manifest validation、drift detection | manifest valid；5 项既有 drift 被保留并报告 | 命令标准输出 |

SwiftPM 在 Xcode 26 的 llvm-cov 输出没有 branch 数据，质量门明确记录 unavailable，没有将其伪报为 ≥75%。Ad-hoc signing 的 Xcode 日志提示构建时禁用 hardened runtime；当前证据只覆盖本地签名与 entitlement，不覆盖发布签名安全性。

## 未完成与人工验收

- TextKit：使用系统与微信输入法运行组合/候选/取消/撤销、attachment、100k 字延迟和 VoiceOver 流程，保存可核对记录。
- Store：实现并运行 100k operation replay、kill/reopen、损坏 snapshot fallback 和受限磁盘 `SQLITE_FULL`。
- Skill：执行真实 Wasmtime guest、capability callback、无 preopen/environment、loopback broker、fuel/deadline、取消和 crash/reconnect 矩阵。
- Benchmark：生成并校验 10k records、100k blocks、20 GiB asset corpus，并使用系统 framework 抽样打开媒体。
- Security/CI：运行日志与产物 sentinel 扫描，并取得远端 GitHub Actions 结果。

在上述门槛完成并经过独立测试、独立评审前，P0 不得标为 PASS 或关闭，P1 不得启动。
