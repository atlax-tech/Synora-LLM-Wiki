# 2026-09-07 · Phase 1 · P1 收口

## 范围与结果

- 阶段：P1 原生壳层与设计系统；结论 `PASS`。P1-01…P1-10 已在 `de2b76f`、`173f43a` 完成，P2–P9 仍未启动。
- 此前把视觉基线确认误当成必须停下来的批准门槛；按用户最新指令直接固化可复现的原生候选图，不再把额外批准当作 blocker。
- 六张 light/dark 三尺寸 PNG 已固定到 `Tests/VisualBaselines/P1`。原生 P1 保留侧栏、记录列表、正文、工具栏和可选检查器；参考中的媒体、地图、天气和 AI 卡片属于后续阶段，不在本阶段伪装实现。

## 实际验证

| 检查 | 结果 |
|---|---|
| `swift build` / `swift test --package-path Packages/SynoraCore --skip SynoraStoreHeavyTests`（warnings as errors） | PASS，SwiftPM 26/26 |
| `SYNORA_ARTIFACT_DIR=/private/tmp/synora-wiki-p1-stage-final SYNORA_DERIVED_DATA=/private/tmp/synora-wiki-p1-stage-final/xcode ./script/p1.sh stage` | PASS，产品单测 16/16、UI 测试 6/6 |
| `visual-report-light.json` / `visual-report-dark.json` | PASS；六项 SSIM 均 ≥ 0.9996（门槛 0.95） |
| 三尺寸几何 | compact/default 为 `MATCH`；large 宽度匹配，高度 1117→1050 pt，明确记录为 `CLAMPED_ENVIRONMENT` |
| `git diff --check`、严格 `xcrun swift-format lint` | PASS |

视觉报告和 Xcode result bundle 保存在 `/private/tmp/synora-wiki-p1-stage-final`。当前宿主显示器的可见高度只有 1050 pt；阶段脚本只对 large 的宿主高度限制做了显式记录，其他几何、内容区和 SSIM 失败仍会失败。

## 人工复核与交接

- 已对照本机 `local-reference/high-fidelity/` 高保真参考确认信息层级和 P1 范围；不同 viewport、内容和系统 chrome 不做直接 SSIM。
- UI 测试覆盖原生控件 label/identifier、键盘搜索、侧栏折叠、检查器 Context/AI Skills、命令面板、主题切换和记录选择。
- 真实 VoiceOver 旁白、多显示器移动以及发布级可访问性未在本机运行，继续由 P9/对应阶段承接；不把这些未运行项写成已通过。

---

# 2026-09-06 · Phase 1 · P1 启动

## 范围与结果

- 阶段：P1 原生壳层与设计系统；状态由 `READY` 改为 `IN_PROGRESS`。
- 启动提案已获用户明确批准。批准范围包括 PLAN/SPEC/DESIGN、此阶段日志和 Harness 索引指纹。
- 设计决策：macOS 26 系统 Liquid Glass 用于导航与功能控制层；正文、摘要和检查器信息使用 Apple 原生 Material 或语义实色表面；禁止自制模糊、折射和透明叠层。
- 产品范围保持 P1 边界：不提前实现 P2 编辑器、P3 持久化、P4 AI 或 P6 Skill 生命周期。

## 启动验证

- `./script/build_and_run.sh --verify`：PASS，Debug、arm64、macOS 26，bundle id `tech.atlax.SynoraWiki`。
- `swift build` / `swift test`（临时 Swift/Clang cache，跳过 heavy suite）：PASS。
- 产品单测与 `SynoraP0UITests/testApplicationLaunchesWithWindow`：PASS。
- Harness manifest、structure、reference（97 条）、drift：PASS，启动前无 drift。
- 初次沙箱构建曾因网络代理和用户级 Clang cache 不可写失败；改用受控联网权限及临时 cache 后通过，属于环境记录，不是产品缺陷。

## 启动时未验证项与风险（已由上方收口记录更新）

- P1 代码、视觉基线、VoiceOver、三尺寸截图和真实多显示器恢复尚未完成。
- 当前本机全局 Xcode DerivedData 路径不可写；P1 所有命令固定显式临时 artifact/DerivedData。
- 用户本地 `.vscode/` 和 `xcuserdata/` 保留未跟踪，后续提交不得包含。

## 启动时计划（已完成；见上方收口记录）

按 P1-01 → P1-10 顺序实现，每项通过 focused 检查后单独提交；P1-09 三张原生截图在记录基线前交由用户确认。
