## 当前 P2 唯一缺口表

|任务 ID|剩余行为|应用入口|验证与证据|状态|下一动作|
|---|---|---|---|---|---|
|P2-01|现有 schema 回归|BlockDocument|当前包基线|待验证|复用现有参数化测试|
|P2-02|连续编辑与 composition 接线|SynoraTextView|真实输入法及应用测试|未完成|核对事件到会话更新|
|P2-03|基础块可见编辑|RecordEditorView|每类块应用旅程|未完成|核对渲染与操作|
|P2-04|表格、折叠、callout 嵌套|RecordEditorView|高级块应用旅程|未完成|连接现有 EditorSession 能力|
|P2-05|自动快捷输入、引用|正文输入入口|真实键盘操作|未完成|补触发与选择入口|
|P2-06|选区格式、剪贴板|原生文本视图|结构往返及选择验证|未完成|核对格式实际呈现|
|P2-07|附件异常路径|AssetStore|故障注入及真实事务|待验证|复用现有资产测试|
|P2-08|媒体撤销与惰性呈现|媒体卡片|应用操作及重开|未完成|统一会话与保存路径|
|P2-09|保存竞争、历史入口|ShellModel|竞争及恢复测试|未完成|修复事务回写|
|P2-10|模板与元数据编辑|记录编辑页|创建及重开|未完成|核对现有模板服务|
|P2-11|原生导入导出旅程|文件面板|往返及取消失败|待验证|复用 RecordExportService|
|P2-12|真实 IME/VO/屏幕性能|真实应用窗口|三轮性能与系统验收|未完成|前述功能稳定后运行|

# 2026-09-08 03:20 · P 2 · 第 23 步，对应开发计划中的 5 步 · 块结构入口与原生滚动编辑器

## 1. 本轮任务列表

- 将 Return 和块首 Backspace 从原生文本视图接入 `EditorSession`，保留块结构、选区和自动保存路径。
- 为缩进、反缩进、callout 样式及表格行列操作提供真实记录编辑页入口。
- 将正文宿主放入原生 `NSScrollView`，避免长正文只显示固定高度。
- 复跑相关包、应用单元和现有应用 UI 回归；不把这些回归误判为 P2 阶段通过。

## 2. 本轮任务完成度

Return 会按当前块和 UTF-16 选区调用 `pressReturn`，块首 Backspace 会调用 `pressBackspace`；其他删除仍交给原生文本视图。Structure 菜单现在连接已有 EditorSession 操作，文本视图使用原生滚动容器承载长正文。当前 P2 仍未完成，真实 IME、VoiceOver、屏幕性能、导出旅程和附件故障矩阵仍缺证据。

## 3. 测试方式

- `./script/p2.sh unit`，复用 `/private/tmp/synora-p2-active-cache`，构建并行度 2。
- `./script/p1.sh unit` 与 `./script/p1.sh ui`，使用同一独立 DerivedData 和独立测试结果目录。
- 提交前执行 `git diff --check` 与脚本语法检查。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| P2 unit | 1 | `PASS`，59 个测试、0 失败；`/private/tmp/p2-structure-tests` |
| 应用单元测试 | 1 | `PASS`，16 个测试、0 失败；结果保留在 `/tmp/synora-p2-active-cache/xcode/Logs/Test/Test-SynoraWiki-2026.09.08_03-10-04-+0800.xcresult` |
| 应用 UI 回归 | 1 | `PASS`，7 个测试、0 失败，133.526 秒；`/tmp/synora-p2-active-cache/xcode/Logs/Test/Test-SynoraWiki-2026.09.08_03-10-28-+0800.xcresult` |
| `git diff --check` / `zsh -n script/p1.sh script/p2.sh` | 1 | `PASS` |

## 5. 清理与资源记录

前一轮清理后宿主可用空间约 339 GiB；本轮固定缓存当前约 1.2 GiB，未启动 xctrace 或 Instruments 录制。此前 544 个历史 `synora-*` 路径的批量删除被安全审查拒绝，未绕过；仅删除已核验的六个当前任务大产物。阶段结束前继续删除本批可再生缓存并保留最小有效证据。

## 6. 下轮动作

按唯一缺口表继续核对历史、模板、元数据和原生导入导出入口；每个独立修复先补应用验证，再更新本日志并小步提交。真实 IME、VoiceOver 和屏幕性能只在功能入口稳定后按资源上限分段采样。

# 2026-09-08 03:25 · P 2 · 第 24 步，对应开发计划中的 2 步 · 固定 Xcode/SwiftPM 缓存路径

## 1. 本轮任务列表

- 修正 P1 应用测试脚本，使 DerivedData、模块缓存和 SwiftPM checkout 默认落在本任务固定缓存目录。
- 让 P2 调用 P1 UI/视觉入口时继承同一缓存根目录，避免 Xcode 回写用户缓存。

## 2. 本轮任务完成度

`script/p1.sh` 现在使用 `/private/tmp/synora-p2-active-cache`（可由 `SYNORA_P2_CACHE_ROOT` 覆盖），并传入独立 `SourcePackages`、clang/module cache 和 DerivedData；`script/p2.sh` 使用同一固定根目录。一次无提升权限的应用测试因宿主 Xcode 缓存权限被拒绝，改用固定临时缓存的授权测试后通过，未写入用户库。

## 3. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `zsh -n script/p1.sh script/p2.sh` / `git diff --check` | 1 | `PASS` |
| 应用单元测试（固定临时缓存） | 1 | `PASS`，16 个测试、0 失败；`/private/tmp/p2-history-template-app-r5` |

## 4. 清理与下轮动作

本轮未启动采样器；固定缓存继续与应用功能验证共用，批次结束删除可再生内容。下一步提交应用历史、模板和元数据入口及其 UI 验证。

# 2026-09-08 03:30 · P 2 · 第 25 步，对应开发计划中的 9–10 步 · 历史、模板与元数据应用入口

## 1. 本轮任务列表

- 接通现有 `ProductStore` 的 history/version/restore、template 和 metadata 事务到 `ShellModel`。
- 在真实记录编辑页提供 History、Edit metadata、Template 和 Save current as template 入口。
- 保持空库、fixture 和取消路径可操作；冲突或失败继续显示既有保存状态。
- 在应用 UI 回归中确认新增 Record/Structure 菜单可被用户定位。

## 2. 本轮任务完成度

应用层现在可以异步加载记录历史和当前类型模板，恢复版本后重新投影记录、文档和编辑会话；模板复制沿存储事务生成新块 ID；元数据编辑使用受限的 `key=value` 编辑器并保留系统维护字段。记录页元数据行显示用户字段，长正文编辑器仍与同一保存路径共用。真实导出服务入口保持不变。P2 尚未通过，真实 IME、VoiceOver、性能和故障证据仍缺失。

## 3. 测试方式与结果

- P2 unit：59 个测试、0 失败，`/private/tmp/p2-history-template-unit`。
- 应用 unit：16 个测试、0 失败，固定 `/private/tmp/synora-p2-active-cache`，`/private/tmp/p2-history-template-app-r5`。
- UI 回归新增 Record/Structure 菜单可定位的断言；待提交后复跑完整 `SynoraWikiUITests`。
- `git diff --check` 与脚本语法检查在提交前执行。

## 4. 下轮动作

先复跑新增 UI 回归并审查导出/媒体入口的真实文件面板路径，再进入 IME、VoiceOver 和受限屏幕性能采样。保持单一缺口表，不启动 P3 或全库规模测试。

# 2026-09-08 03:32 · P 2 · 第 26 步，对应开发计划中的 6 步 · 单记录导入入口

## 1. 本轮任务列表

- 在记录编辑页增加原生文件面板的 Import record 入口。
- Markdown、HTML 和 Synora package 继续调用 `RecordExportService` 解析；资产沿 `AssetStore` 内容寻址导入后再写入 `ProductStore`。
- 拒绝重复记录 ID、损坏包和未知扩展，失败时保留原记录并显示保存错误。

## 2. 本轮任务完成度

应用已接通单记录导入：Markdown/HTML 读取既有格式合同，package 校验 manifest/checksum 后导入资产、记录和块；导入记录 revision 从新事务开始，块结构和元数据由服务保留。UI 菜单加入可定位的 Import record 项。当前尚未把导出文件面板和导入重开做成最终验收证据，P2 仍未完成。

## 3. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| 应用单元测试 | 1 | `PASS`，16 个测试、0 失败；`/private/tmp/p2-import-app` |
| `git diff --check` | 1 | 待本步提交前执行 |
| 真实导入/导出文件面板 | 0 | 待用隔离库手工验证 |

## 4. 下轮动作

复跑完整应用 UI 回归；随后用隔离库验证 Markdown、HTML、package 导出/导入、取消、重复和损坏错误路径，记录原始文件与重开结果并及时删除可再生副本。

补充：本轮完整 UI 回归唯一失败是测试在未展开 Record 菜单时查找其内部 Import record 标识；该菜单项按真实层级应在展开后访问，已移除错误的无条件断言，应用代码未因该失败修改。

# 2026-09-08 03:36 · P 2 · 第 27 步，对应开发计划中的 2 步 · 手工运行脚本资源边界

## 1. 本轮任务列表

- 让 `script/build_and_run.sh` 与 P1/P2 测试脚本共用固定 DerivedData、SourcePackages 和模块缓存。
- 默认构建并行度限制为 2，手工运行产物默认落在 `/private/tmp` 的任务目录。

## 2. 本轮任务完成度

手工运行脚本现在不会因 `TMPDIR` 而把构建缓存写进系统用户缓存；可用路径仍可通过任务专用环境变量显式覆盖。该脚本只负责构建和启动应用，手工验证结束后由本任务显式终止应用并清理缓存。

## 3. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `zsh -n script/build_and_run.sh` / `git diff --check` | 1 | `PASS` |

## 4. 下轮动作

提交脚本生命周期修复后，使用隔离库和最小媒体样本验证导入、导出、取消、重复和损坏路径；不使用长时 Instruments。

# 2026-09-08 02:51 · P 2 · 第 21 步，对应开发计划中的 1–2 步 · 缓存清理与资源约束

## 1. 本轮任务列表

- 明确清单清理 368 个构建和模块缓存，保留日志及测试结果。
- 撤销本任务五项测试启动偏好。
- AGENTS 增加全阶段产物生命周期；P2 复用缓存、单任务锁、资源预算与退出回收，构建并行度 2。

## 2. 本轮任务完成度

清理实际释放 133.27 GiB，完成时可用 289.87 GiB；清单与旧证据位于 `/private/tmp/p2-retained-evidence`。旧 trace 和其他未分类产物尚未全部处理。P2 未完成。

## 3. 测试方式

执行 zsh 语法检查、git diff --check；未运行构建、应用或性能测试。

## 4. 测试结果

语法及差异检查通过。使用临时 stub swift 验证成功与退出码 7 两条路径，均正确清理缓存和锁；未执行产品测试。资源容量超限路径尚未验证。

## 5. 用户如何进行手工测试

正常启动应用应使用正常库；检查可用磁盘空间。

## 6. 风险预估与规避

保留旧测试结果，未删除真实用户库；缓存清理后首次构建会重新编译。

## 7. 下轮建议

验证资源脚本，再按缺口表收敛编辑旅程。

## 8. 本轮任务大白话讲解

先释放测试堆积占用，恢复正常启动设置，限制后续测试资源。

# 2026-09-08 00:09 · P 2 · 第 18 步，对应开发计划中的 12 步 · 原生工具栏状态回归修复

## 1. 本轮任务列表

- 复跑当前 P1 原生 UI 回归，定位工具栏项在连续测试中消失的真实原因。
- 保留用户自定义工具栏行为，只在 UI 测试启动时清除旧的 `NSToolbar Configuration shell-toolbar` 状态。
- 以单测试验证搜索、上下文、AI Skills、Inspector 和 Command Palette 入口可被用户操作和辅助功能定位。

## 2. 本轮任务完成度

回归根因是本机应用偏好中保留了之前隐藏的可自定义工具栏项；`-ApplePersistenceIgnoreState` 不会清除该配置，导致测试只看到 Command Palette。`ShellRootView` 现在仅在 `SYNORA_UI_TESTING=1` 时移除该测试状态，生产会话仍保留用户的工具栏定制。聚焦工具栏 UI 测试已通过，P2 阶段仍需完整 UI、真实 VoiceOver、屏幕性能和故障矩阵证据。

## 3. 测试方式

- 使用独立 DerivedData 和本地 package checkout 构建 `SynoraWiki`。
- 运行 `-only-testing:SynoraWikiUITests/SynoraWikiUITests/testToolbarToolsAreIndependentRightSideItems`。
- 提交前执行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| 聚焦工具栏 UI 回归 | 1 | `PASS`，1 个测试、0 失败；xcresult `/tmp/synora-p2-toolbar-test/Logs/Test/Test-SynoraWiki-2026.09.08_00-07-38-+0800.xcresult` |
| `git diff --check` | 1 | `PASS` |

## 5. 未闭合证据

完整 `SynoraWikiUITests`、编辑器新入口的用户旅程、真实 VoiceOver 朗读、100k/200 媒体屏幕 P95，以及磁盘满、权限失败、损坏附件和中断场景仍需继续验证；当前阶段出口继续为 `CHANGES_REQUIRED`。

# 2026-09-08 00:03 · P 2 · 第 17 步，对应开发计划中的 12 步 · 编辑宿主出口与原生导出

## 1. 本轮任务列表

- 将持久化 `BlockDocument` 和 `EditorSession` 接入应用正文宿主，保留选区、组合输入、撤销重做和稳定块身份。
- 暴露块类型、Markdown 快捷方式、格式、查找替换、任务/折叠操作，并把媒体说明、排列和布局写回同一事务路径。
- 复用 `RecordExportService` 提供 Markdown、HTML、PDF 和原始 Synora package 的原生保存面板入口。

## 2. 本轮任务完成度

应用正文已经不再只传递字符串：`SynoraTextView` 报告真实选区，`ShellModel` 以 `EditorSession` 增量应用编辑并安排自动保存；编辑工具直接调用已有 EditorKit 命令，未知属性和块 ID 继续由 `BlockDocument` 保留。媒体卡片现在能编辑 caption、选择 single/collage/gallery 布局并按存储顺序呈现。导出菜单通过原生 `NSSavePanel` 调用既有 `RecordExportService`，取消保存不会写入。P2 阶段仍需真实 IME、VoiceOver、屏幕性能和故障矩阵证据，未宣称阶段通过。

## 3. 测试方式

- 使用本地依赖和独立 DerivedData 构建：
  `GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig SWIFT_PACKAGE_DISABLE_SANDBOX=1 CLANG_MODULE_CACHE_PATH=/private/tmp/synora-p2-xcode-clang SWIFT_MODULECACHE_PATH=/private/tmp/synora-p2-xcode-swift xcodebuild -project SynoraWiki.xcodeproj -scheme SynoraWiki -configuration Debug -sdk macosx -derivedDataPath /private/tmp/synora-p2-editor-app -clonedSourcePackagesDirPath /private/tmp/synora-xcode-sourcepackages build`
- 运行同一构建目录的 `-only-testing:SynoraWikiTests test`。
- 使用独立 SwiftPM scratch 运行 `./script/p2.sh unit` 与 `./script/p2.sh integration`。
- 通过 `git diff --check` 检查提交前差异。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Xcode Debug App build（编辑与导出接线） | 1 | `PASS`，`** BUILD SUCCEEDED **`；产物 `/private/tmp/synora-p2-editor-app/Build/Products/Debug/SynoraWiki.app` |
| `SynoraWikiTests` | 1 | `PASS`，16 个测试、0 失败；xcresult `/tmp/synora-p2-editor-app/Logs/Test/Test-SynoraWiki-2026.09.08_00-01-38-+0800.xcresult` |
| P2 unit | 1 | `PASS`，58 个测试、0 失败 |
| P2 integration | 1 | `PASS`，26 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

## 5. 未闭合证据

原生入口已具备，但真实 IME 组合输入、VoiceOver 朗读、100k/200 媒体窗口的输入/首屏/滚动 P95，以及磁盘满、权限失败、损坏附件和中断场景仍需在干净运行会话中采集；当前阶段出口继续为 `CHANGES_REQUIRED`。

# 2026-09-07 22:50 · P 2 · 第 16 步，对应开发计划中的 12 步 · 媒体替换回调与最终阶段探针

## 1. 本轮任务列表

- 修正 `RecordEditorView` 的原生文件替换回调，保证 Open 面板完成后仍保留目标 block/asset，并让取消选择不会误写。
- 用当前媒体宿主重建 Debug App，重跑 `SynoraWikiTests` 与 `SynoraAssetsTests`，确认状态修复没有破坏应用和资产层。
- 在隔离 App Sandbox 数据库上复核真实删除/替换 AX 结果与 `document.save` 操作，再运行 P2 `stage` 生成最终探针报告。
- 继续把 VoiceOver、屏幕 P95、导出 UI、文件系统故障矩阵和最新 XCUITest 的缺证据保留为未验证，不修改阶段状态。

## 2. 本轮任务完成度

媒体替换的根因是之前把 `fileImporter` 的 `isPresented` 绑定写成了带副作用的计算 `Binding`；系统关闭 Open 面板时 setter 会先清空 `replacementTarget`，完成回调因此没有目标。改为独立的 `@State replacingAttachment`，回调先捕获并清理目标再执行替换后，真实窗口中已看到 `Gallery: media-195.png` 和新的资产 ID。此前同一隔离库的删除操作也已从 AX 树移除 `media-0`，并留下可恢复的未引用资产。P2 仍为 `CHANGES_REQUIRED`。

## 3. 测试方式

- 使用本地依赖和独立 DerivedData 构建：
  `GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig xcodebuild -project SynoraWiki.xcodeproj -scheme SynoraWiki -configuration Debug -sdk macosx -derivedDataPath /private/tmp/synora-p2-app-build-media10 -clonedSourcePackagesDirPath /private/tmp/synora-xcode-sourcepackages build`
- 使用同一构建目录运行 `-only-testing:SynoraWikiTests test`；使用独立 SwiftPM scratch 运行 `SynoraAssetsTests`。
- 关闭并重开隔离容器中的真实 App，通过 AX 树检查 Replace/Remove 和媒体预览；数据库查询资产、操作和最新 revision。
- 运行 `SYNORA_ARTIFACT_DIR=/private/tmp/synora-p2-stage-final2 SYNORA_P2_GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig ./script/p2.sh stage`；不注入 IME、VoiceOver 或 Instruments 证据环境变量。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Xcode Debug App build（替换状态修复后） | 1 | `PASS`，`** BUILD SUCCEEDED **` |
| `SynoraWikiTests`（当前代码） | 1 | `PASS`，16 个测试、0 失败；xcresult 为 `/tmp/synora-p2-app-build-media10/Logs/Test/Test-SynoraWiki-2026.09.07_22-46-19-+0800.xcresult` |
| `SynoraAssetsTests`（媒体协调器/资产回归） | 1 | `PASS`，8 个测试、0 失败 |
| CUA 删除复核 | 1 | `PASS`，AX 移除 `Gallery: media-0.png`；隔离库操作 revision 到 211，资产保留用于恢复 |
| CUA 替换复核 | 1 | `PASS`，Open 面板选择 `media-195.png` 后 AX 显示 `Gallery: media-195.png`；隔离库 `assets=202`、`operations=212`、最新 `document.save` revision 212 |
| P2 stage unit / integration | 1 | `PASS`，58 / 26 个测试、0 失败 |
| SIGKILL recovery probe | 1 | `PASS`，writer exit 137 后 `P2_RECOVERY_CHECK=PASS`，`operations=511`、`revision=511` |
| 100k 字/200 媒体性能探针 | 1 | `MODEL_ONLY`，UTF-16 长度 150000、媒体 200（图片/PDF/音频 67/67/66），edit P95 `0.170 ms`、reopen `3.111 ms`、save `15205.346 ms`；不是屏幕绘制证据 |
| P2 stage 出口 | 1 | `CHANGES_REQUIRED`；UI/visual skipped，real IME/VoiceOver/keystroke-to-paint 均 `UNVERIFIED` |
| `git diff --check` / `zsh -n script/p2.sh` | 1 | `PASS` |

## 5. 用户如何进行手工测试

在隔离库打开已有媒体记录，点击任一媒体的 `Replace`，在原生 Open 面板选择另一图片或文件；面板完成后应看到同一媒体位置显示新文件名，取消选择不应改变文档。点击 `Remove` 后媒体块从 AX 树消失，关闭再开仍保持文档结构。当前 UI 没有导出按钮；导出服务的 SwiftPM round-trip 通过，但应用导出旅程仍需补验。

## 6. 风险预估与规避

- **替换目标丢失（已修复）**：展示状态和完成回调现在由独立布尔状态控制，回调先捕获目标，避免 `fileImporter` 关闭时清空目标。
- **未引用资产回收（中）**：删除/替换不会立即删除旧原件，数据库仍保留资产以支持历史和恢复；垃圾回收矩阵留给后续容量阶段。
- **性能证据不足（高）**：模型探针只证明领域编辑、解码和 SQLite 保存规模；没有把 `editP95` 或一次 290 ms signpost 样本写成 keystroke-to-paint P95。
- **产品门槛未闭合（高）**：真实 VoiceOver 朗读、100k/200 媒体首屏/滚动、导出 UI、磁盘满/权限撤销/损坏媒体和最新 XCUITest 仍无完整证据。

## 7. 下轮建议

在清洁会话完成仅键盘 VoiceOver 媒体旅程，分段采集可读的多次 `keystrokeToPaint`/首屏/滚动样本，补导出 UI 与文件系统故障矩阵；所有质量门通过后再更新 P2 阶段状态。当前不提交另一会话的 App/Xcode 工作树差异，也不推送。

## 8. 本轮任务大白话讲解

媒体替换之前会在系统文件面板关闭的一瞬间丢掉目标，导致用户选完文件却没有变化。现在目标状态独立保存，选完文件后能在真实窗口里看到新媒体，删除也能留下正确的操作记录。底层 58/26 测试、恢复探针和模型规模探针通过，但读屏、屏幕速度、导出按钮和故障矩阵仍未达到 P2 阶段出口。

# 2026-09-07 22:05 · P 2 · 第 15 步，对应开发计划中的 12 步 · keystroke-to-paint signpost 通道修正

## 1. 本轮任务列表

- 修正 `EditorTelemetry` 的 `OSLog` 类别为 `PointsOfInterest`，让 Instruments Points of Interest 记录器能够看到编辑器的 `keystrokeToPaint` signpost。
- 在实际运行的长文/200 媒体窗口上 attach `Animation Hitches + Points of Interest`，导出应用进程的 Begin/End 行并计算完整样本时延。
- 继续保留 Instruments 长时保存器的真实失败，不把单个样本或失败 trace 扩大成 P2 性能通过。

## 2. 本轮任务完成度

类别修正后，attach trace `/private/tmp/synora-p2-points-attach-20260907.trace` 已导出 `tech.atlax.SynoraWiki / PointsOfInterest / keystrokeToPaint` 的 Begin/End 事件；第一个完整样本为约 `290.192 ms`。25 秒多样本录制的保存器因 Instruments “too many facts” 失败，但 trace 仍被保存且没有可用行。当前证据明确显示至少一个样本远超 16 ms，P2 继续为 `CHANGES_REQUIRED`。

## 3. 测试方式

- 用本地依赖重新构建 Debug App，产物为 `/private/tmp/synora-p2-app-build-media6/Build/Products/Debug/SynoraWiki.app`。
- 对运行中的 PID `50118` 使用 `xcrun xctrace record --template 'Animation Hitches' --instrument 'Points of Interest' --attach 50118 --time-limit 10s`，CUA 在 `editor-body` 输入字符。
- 通过 XPath 导出 `os-signpost` 表，按同一 signpost ID 的 Begin/End 时间差计算完整样本；另一次 25 秒采样用于检查重复输入稳定性。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `EditorTelemetry` 类别修正后的 Xcode build | 1 | `PASS`，`** BUILD SUCCEEDED **` |
| `SynoraEditorKitTests`（类别修正后） | 1 | `PASS`，16 个测试、0 失败 |
| attach trace 目标进程 | 1 | `PASS`，`SynoraWiki (pid 50118)` |
| `keystrokeToPaint` 完整样本 | 1 | `PASS`，Begin `00:04.614890541`、End `00:04.905082875`，约 `290.192 ms` |
| 25 秒多样本 trace 保存 | 1 | `UNVERIFIED`，Instruments modeler 报 “too many facts”，虽保存 trace 但没有可用 signpost 行 |
| `script/p2.sh stage`（telemetry/媒体宿主改动后） | 1 | `CHANGES_REQUIRED`，unit 58、integration 26、recovery PASS、performance 报告生成；真实 IME/VoiceOver/屏幕 P95 仍未满足 |
| P2 quality gate | 1 | `CHANGES_REQUIRED`，完整 100k 字/200 媒体 P95 仍未建立，且已有样本超过 16 ms |

## 5. 用户如何进行手工测试

在干净窗口打开 100k 字/200 媒体记录，用包含 Points of Interest 的 Instruments 模板 attach 目标进程，连续输入并导出 `keystrokeToPaint` signpost。至少需要多次完整 Begin/End 样本、滚动和首屏数据，才能计算 P95；本轮单个样本只用于暴露明显延迟。

## 6. 风险预估与规避

- **首个样本偏慢（高）**：`290.192 ms` 可能包含首次输入/布局或保存开销，不能外推为 P95；但已经足以阻止性能门误报通过。
- **Instrumentation 保存器故障（高）**：长时录制遇到 Instruments modeler 内部事实数量上限，需缩短窗口或降低记录器范围后重试。
- **质量门边界（高）**：signpost 现在可见不等于 VoiceOver、连续滚动、首屏或 200 媒体稳定性已通过。

## 7. 下轮建议

降低 Points of Interest 录制范围并分段采集多次输入、首屏和滚动样本；修复或解释首次 290 ms 延迟后，再用完整窗口计算 P95，并补真实 VoiceOver 与媒体替换/导出故障矩阵。

## 8. 本轮任务大白话讲解

以前 Instruments 看不到编辑器的自定义计时，是因为日志类别不在 Points of Interest 通道。现在通道已接通，真实输入样本显示第一次绘制约 290 ms，性能门不能通过；长时间采集还触发了 Instruments 自身保存限制，需要分段重测。

# 2026-09-07 21:34 · P 2 · 第 14 步，对应开发计划中的 12 步 · Instruments 直接进程采样复核

## 1. 本轮任务列表

- 用 `Animation Hitches` 模板直接启动带隔离长文/媒体库的 `SynoraWiki` 进程，避免把 `/usr/bin/env` 包装进程误当作应用目标。
- 导出 trace 目录和 hitches 相关表，确认采样是否包含应用进程、可识别的卡顿行和自定义 `keystrokeToPaint` 事件。
- 只把实际可读的采样事实写入证据边界，不把显示刷新统计或空表扩大为 P2 性能通过。
- 修正 `RecordMediaStack` 初次渲染时同步解析原件的问题，让首屏先显示占位并等待预览协调器异步结果。

## 2. 本轮任务完成度

直接目标进程采样已完成，trace 为 `/private/tmp/synora-p2-hitches-direct-20260907.trace`。`SynoraWiki` 被 Instruments 识别并运行约 12.66 秒；导出的 `hitches-summary` 和 `hitches-render-interval` 没有应用卡顿行，也没有可导出的 `keystrokeToPaint` signpost。媒体宿主随后移除首屏同步 `AssetStore.preview` 调用，并以本地依赖重新构建通过。该结果只能说明本次短时采样未取得可用 P95，P2 继续为 `CHANGES_REQUIRED`。

## 3. 测试方式

- 使用 `xcrun xctrace record --template 'Animation Hitches' --env ... --launch -- <SynoraWiki executable>`，环境变量指向隔离 `p2-media-ui` 数据库和资产目录。
- 使用 `xcrun xctrace export --toc` 确认目标进程、macOS 版本、采样时长和模板；使用 XPath 导出 `hitches-summary`、`hitches-render-interval` 与 `hitches-lifetime-interval`。
- CUA 在正文 AX 元素中输入一个字符，作为实际窗口活动；不修改生产库。
- 使用本地 package checkout 重新构建 Debug App，验证媒体首屏异步预览改动。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `Animation Hitches` 直接目标启动 | 1 | `PASS`，目标为 `SynoraWiki`，12.663812 秒，trace 成功保存 |
| 目标进程识别 | 1 | `PASS`，trace TOC 含 `SynoraWiki` 及隔离库/资产环境 |
| `hitches-summary` / `hitches-render-interval` | 1 | `UNVERIFIED`，只有 schema、没有可用应用卡顿行 |
| `keystrokeToPaint` signpost | 1 | `UNVERIFIED`，当前模板导出未包含该自定义 signpost |
| 媒体首屏异步预览改动的 Xcode build | 1 | `PASS`，`** BUILD SUCCEEDED **` |
| `SynoraWikiTests`（异步预览改动后） | 1 | `PASS`，16 个测试、0 失败，xcresult 位于 `/private/tmp/synora-p2-app-unit-media5/Logs/Test/Test-SynoraWiki-2026.09.07_21-41-45-+0800.xcresult` |
| CUA 最终 App 媒体宿主复核 | 1 | `PASS`，AX 树恢复 `Gallery: media-0.png`、`media-1.pdf`、`media-2.wav`、`media-3.png`、`media-4.pdf`，音频控件可见 |
| P2 quality gate | 1 | `CHANGES_REQUIRED`，尚无 100k 字/200 媒体 keystroke-to-paint P95 |

## 5. 用户如何进行手工测试

在 Instruments 中选择 `Animation Hitches` 或包含 Points of Interest 的模板，直接启动带测试库的 Debug App；编辑长文并连续滚动后导出目标进程的卡顿与 signpost 表。只有取得可重复的屏幕事件和 P95，才能与 `TESTING.md` 的 16 ms 预算比较。

## 6. 风险预估与规避

- **空闲/短采样误读（高）**：本次采样只覆盖一次启动和一次字符输入，不能代表连续编辑、滚动或 200 媒体稳定性。
- **模板覆盖不足（高）**：`Animation Hitches` 没有导出自定义 signpost；需要包含 Points of Interest 的专用录制或后处理，不能用空表声称通过。
- **全局显示统计误读（高）**：display surface 计数包含系统显示活动，不作为应用首屏或 keystroke-to-paint 指标。

## 7. 下轮建议

在清洁测试会话选择能导出 Points of Interest 的模板，先完成可读的 `keystrokeToPaint` trace，再连续输入/滚动 100k 字、200 媒体窗口；随后补真实 VoiceOver、替换/导出和故障矩阵，完成全部 P2 门槛后再改阶段状态。

## 8. 本轮任务大白话讲解

这次确实让 Instruments 盯住了 SynoraWiki 进程，也保存了 trace，但短时模板没有产出能用来计算输入到屏幕绘制延迟的表格。现有数据证明采样链路可启动，不证明长文媒体性能达标，因此阶段仍待补验。

# 2026-09-07 21:22 · P 2 · 第 13 步，对应开发计划中的 12 步 · 媒体宿主、沙盒附件导入与恢复复核

## 1. 本轮任务列表

- 修正应用媒体宿主接入后的编译问题，补齐 `SynoraAssets` 依赖、附件导入入口、图片/PDF/音频/文件预览和块级辅助功能语义。
- 修正沙盒应用的用户选中文件只读 entitlement，使 SwiftUI 文件选择器能在真实窗口打开并交给附件管线。
- 用隔离 App Sandbox 库从真实原生 Open 面板导入一张图片，检查资产、块和操作事务，再退出重开验证媒体引用恢复。
- 复跑 `SynoraCore` P2 focused 回归、`zsh` 语法和差异检查；尝试复跑原生 XCUITest，保留自动化模式超时这一失败事实。

## 2. 本轮任务完成度

应用媒体宿主、沙盒附件选择和一次真实图片导入/重开链路已接通；证据文件为 `/private/tmp/synora-p2-media-evidence.txt`。P2 阶段仍为 `CHANGES_REQUIRED`：真实 VoiceOver 朗读、Instruments keystroke-to-paint P95、200 媒体连续滚动/输入、替换与导出 UI、磁盘/权限/损坏媒体矩阵尚未完成。

## 3. 测试方式

- 使用本地 Swift package checkout 构建 Debug App：
  `GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig xcodebuild -project SynoraWiki.xcodeproj -scheme SynoraWiki -configuration Debug -sdk macosx -derivedDataPath /private/tmp/synora-p2-app-build-media4 -clonedSourcePackagesDirPath /private/tmp/synora-xcode-sourcepackages build`
- 检查签名 entitlement：`codesign -d --entitlements :- /private/tmp/synora-p2-app-build-media4/Build/Products/Debug/SynoraWiki.app`。
- 使用 CUA 在真实窗口点击 `editor-add-attachment`，通过原生 `open-panel` 的 GoTo 路径选择 `/private/tmp/synora-p2-media-ui/sources/media-195.png`；用 `sqlite3` 查询资产、块和操作数量，退出后以同一隔离路径重开并读取 AX 树。
- 运行 `SYNORA_P2_GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig ./script/p2.sh unit`、应用单元测试 `SynoraWikiTests`、`zsh -n script/p2.sh` 和 `git diff --check`。
- 在清理隔离应用后尝试两次 `SynoraWikiUITests`；两次均在测试开始前因 XCTest `Timed out while enabling automation mode` 失败，未把它改写成通过证据。此前壳层 6/6 通过记录保留在上一轮。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Xcode Debug App build（媒体宿主 + entitlement） | 1 | `PASS`，`** BUILD SUCCEEDED **` |
| 签名 entitlement | 1 | `PASS`，`app-sandbox=true`、`user-selected.read-only=true` |
| CUA 原生文件选择器与图片导入 | 1 | `PASS`，`assets=201`、`blocks=152`、`operations=202`，最新资产为 `media-195.png` |
| 退出后重开媒体恢复 | 1 | `PASS`，AX 树出现 `Image: media-195.png`、`Details: Editable` |
| P2 unit | 1 | `PASS`，58 个测试、0 失败 |
| 应用单元测试 `SynoraWikiTests` | 1 | `PASS`，16 个测试、0 失败 |
| `zsh -n script/p2.sh` | 1 | `PASS` |
| `git diff --check` | 1 | `PASS` |
| `SynoraWikiUITests` 最新构建 | 2 | `UNVERIFIED`，均为 XCTest automation mode 环境超时 |
| P2 quality gate | 1 | `CHANGES_REQUIRED`，VoiceOver、Instruments、200 媒体 UI 和故障矩阵仍未验证 |

## 5. 用户如何进行手工测试

用带本地 package checkout 的 Debug App 打开真实本地记录，点击 `Add attachment`，在 Open 面板选择任意图片/PDF/音频文件；导入完成后应看到对应媒体块，关闭应用再打开后引用仍存在。正文 AX 元素应提供 `editor-body`、`Record body` 和 `Edit the selected record`，媒体块应提供媒体类型、文件名和 `Editable` 状态。完整 VoiceOver 键盘流程与长文滚动性能仍需在专门验收环境执行。

## 6. 风险预估与规避

- **附件选择器不可见（已修复）**：沙盒应用缺少用户选中文件只读 entitlement；补上后原生 `open-panel` 可见，导入后原件仍由 `AssetStore` 内容寻址保存。
- **导入事务与恢复（已确认一条真实路径）**：数据库新增一条资产、一条媒体块和一条操作，进程退出重开后 AX 仍显示新媒体；未扩展成全部媒体类型和损坏/权限故障矩阵。
- **AX 证据被误读（高）**：AX 树能定位正文和媒体编辑状态，但不等于 VoiceOver 实际朗读通过；阶段脚本继续保留 `realVoiceOver=UNVERIFIED`。
- **性能数字被误读（高）**：领域探针和 signpost 代码仍不等于 100k 字/200 媒体窗口的屏幕 P95；本轮没有填充 Instruments 结果。
- **自动化环境失败（中）**：两次 XCUITest 在启动自动化模式前超时，旧的 6/6 壳层通过记录不代表当前改动已获得新的 UI 回归通过。

## 7. 下轮建议

先在清洁测试会话恢复 XCUITest 自动化，再补充 VoiceOver 仅键盘媒体旅程与 Instruments keystroke-to-paint/首屏/滚动采样；随后覆盖图片、PDF、音频和文件的导入/替换/导出以及磁盘满、权限撤销、损坏媒体和进程终止矩阵。所有证据齐全后再更新 P2 阶段状态。

## 8. 本轮任务大白话讲解

现在应用里的正文可以编辑，附件按钮会打开系统文件选择器；选一张图片后，系统把原件复制进本地资产库，并把图片块和记录更新写进同一条保存操作。退出再打开，图片仍然能被找到并显示为可编辑媒体。输入法边界已经有真实证据，读屏实际朗读、长文和 200 个媒体的屏幕速度还没有足够的测量，所以阶段继续待补验。

# 2026-09-07 20:45 · P 2 · 第 12 步，对应开发计划中的 12 步 · IME 组合提交边界与质量门复核

## 1. 本轮任务列表

- 追踪真实 AppKit 输入法路径，确认中文拼音组合文本不会在 marked text 尚未提交时进入 `ProductStore`。
- 在共享 `SynoraTextView` 输入层拦截 `setMarkedText`、`unmarkText` 和组合提交，保留最后已发送文本并在绘制完成时结束 `keystrokeToPaint` signpost。
- 为编辑器正文提供可被辅助功能定位的 `editor-body` 标识、描述和编辑帮助，避免只给 SwiftUI 外层贴标签。
- 用新构建的 App 和隔离 App Sandbox 数据库复测中文输入、提交、退出后重开与数据库完整性；不把 AX 树证据扩大为 VoiceOver 朗读通过。

## 2. 本轮任务完成度

编辑器 IME 持久化边界与正文 AX 接线已完成；修复位于 `Packages/SynoraCore/Sources/SynoraEditorKit/EditorKit.swift`。真实系统输入法证据已取得，P2 阶段出口仍为 `CHANGES_REQUIRED`：VoiceOver 朗读流程、Instruments keystroke-to-paint P95、完整 attachment 宿主和长文/200 媒体 UI 仍未通过。

## 3. 测试方式

- 使用本地 Swift package checkout 构建 Debug App：
  `GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig xcodebuild -project SynoraWiki.xcodeproj -scheme SynoraWiki -configuration Debug -sdk macosx -derivedDataPath /private/tmp/synora-p2-app-build-current -clonedSourcePackagesDirPath /private/tmp/synora-xcode-sourcepackages build`
- 使用本地 module cache 运行 `SynoraEditorKitTests`，并执行 `zsh -n script/p2.sh`、`git diff --check`。
- 在隔离库 `p2-ime-evidence.sqlite` 中先保留 10,000 条恢复操作，再用真实 SCIM Pinyin 输入 `zhongwen`；分别查询组合中和提交后的 `operations` 及 payload，最后关闭 App 并运行 `SynoraP2Recovery --check`。
- AX 证据文件：`/private/tmp/synora-p2-ime-evidence.txt`。其中只记录实际看到的 AX 描述和 SQLite 结果；没有设置伪造的 VoiceOver 或 Instruments 环境变量。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `SynoraEditorKitTests`（最终修复） | 1 | `PASS`，16 个测试、0 失败 |
| `zsh -n script/p2.sh` | 1 | `PASS` |
| `git diff --check` | 1 | `PASS` |
| Xcode Debug App build（本地依赖） | 1 | `PASS`，`** BUILD SUCCEEDED **` |
| SCIM Pinyin marked-text 输入 | 1 | `PASS`：组合显示 `zhong wen` 时操作数保持 10001；空格提交后新增唯一 `document.save`，正文为 `中文` |
| 退出后恢复完整性 | 1 | `PASS`，`P2_RECOVERY_CHECK=PASS operations=10002 revision=10002` |
| P2 quality gate | 1 | `CHANGES_REQUIRED`：AX 树已可定位正文，但真实 VoiceOver 朗读、Instruments 绘制 P95、媒体编辑宿主、磁盘/权限/损坏媒体矩阵仍 `UNVERIFIED` |

## 5. 用户如何进行手工测试

构建 App 后打开一个本地记录，切换到 SCIM 拼音输入法，在正文中逐键输入 `zhongwen`；组合期间应看到拼音但关闭/重开数据库不会出现拼音中间态，按空格提交后只保留 `中文`。用辅助功能检查正文应看到 `editor-body`、`Record body` 和 `Edit the selected record`。VoiceOver 的实际朗读顺序、媒体块说明和可编辑状态仍需在启用 VoiceOver 的环境中完成一次完整键盘流程。

## 6. 风险预估与规避

- **组合文本误持久化（高）**：持久化回调现在统一经过输入视图的 marked-text 状态和最后发送值；真实回归确认组合期间操作数不变，提交只产生一条保存操作。
- **AX 证据被误读（高）**：正文 AX label/id/help 已确认，但 AX 树不等于 VoiceOver 朗读证据，阶段脚本继续将 `realVoiceOver` 标为 `UNVERIFIED`。
- **绘制性能被误读（高）**：代码只埋入 `keystrokeToPaint` signpost，未用 Instruments 采样 100k 字/200 媒体窗口，因此不填充 P95 结果。
- **媒体范围越界（高）**：领域层媒体与资产管线已有 focused 证据，应用壳层仍未接入真实附件导入、替换、AV/PDF/Quick Look 和导出全流程，继续保留质量门。

## 7. 下轮建议

在启用 VoiceOver 的真实窗口中补完仅键盘的新建、编辑、媒体说明和恢复旅程；随后用 Instruments 采集 100k 字/200 媒体的 keystroke-to-paint P95、首屏和滚动，并接入附件导入/替换/导出与磁盘/权限/损坏媒体故障矩阵。所有证据通过后再更新 P2 阶段状态。

## 8. 本轮任务大白话讲解

中文输入法会先把拼音放在一个临时组合层里，用户选字或按空格确认后才把最终汉字交给保存系统；现在这个边界在真实 App 里已经成立，退出再打开仍能恢复已确认的中文。编辑器正文也有稳定的无障碍名称和定位信息。媒体界面、读屏完整流程和屏幕绘制速度还没有足够的实机证据，所以阶段状态继续保持待补验。

# 2026-09-07 19:27 · P 2 · 第 10 步，对应开发计划中的 9～12 步 · P2-09B～P2-12 编辑质量门

## 1. 本轮任务列表

- 在真实 `ProductStore` 事务上补齐 P2-09B 自动保存、持久 undo/redo、history、跨重开动作栈重建和版本恢复；自动保存冲突保留本地草稿，不用内存 mock 代替持久化证据。
- 为 P2-10 增加手帐/笔记模板的保存、查询、复制、删除和安全套用，以及记录元数据行的同一事务更新、撤销和重做。
- 为 P2-11 完成单记录 Markdown、HTML、PDF 和原始包导出/导入；可读格式携带结构化 payload，原始包校验资产路径、字节数、哈希和未知字段，外部 Markdown/HTML 只在 payload 可信时恢复结构。
- 为 P2-12 增加可重复的 100k+ UTF-16 文本/200 媒体性能探针、SIGKILL 恢复探针和 `script/p2.sh` stage 汇总；另外运行现有原生壳层 XCUITest，区分壳层旅程证据与真实 IME、VoiceOver、keystroke-to-paint 和媒体宿主验收。
- 保留另一会话的 7 个 App/Xcode 未提交文件，不修改 `PLAN.md`、阶段状态或全局代理配置。

## 2. 本轮任务完成度

代码与可重复探针范围 `100%`：提交 `c664f9a`、`f155c36`、`45cd6f3` 和 `3b8c9f5` 已完成 P2-09B、P2-10、P2-11 以及 P2-12 的本地探针/脚本范围。阶段出口仍为 `CHANGES_REQUIRED`，不能把领域层性能数字或壳层 XCUITest 直接记作 P2 通过；真实系统 IME、VoiceOver、keystroke-to-paint、完整 attachment 宿主和长文媒体 UI 仍未验收。

## 3. 测试方式

- 使用本地 GRDB/SQLite URL 映射、独立 SwiftPM scratch、临时 Swift/Clang module cache 和临时数据库运行 `script/p2.sh stage`；命令为：
  - `SYNORA_ARTIFACT_DIR=/private/tmp/synora-p2-stage-final SYNORA_P2_GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig ./script/p2.sh stage`
- 运行 `zsh -n script/p2.sh` 和 `git diff --check`；P2 产品/脚本只暂存 `Package.swift`、两个可执行探针和 `script/p2.sh`，既有 7 个 App/Xcode 文件继续未暂存。
- `SynoraWikiUITests` 使用本地 Xcode source package 缓存和独立 DerivedData 运行；结果日志为 `/private/tmp/synora-p2-ui-local-escalated/ui.log`，xcresult 为 `/private/tmp/synora-p2-ui-local-escalated/xcode/Logs/Test/Test-SynoraWiki-2026.09.07_19-18-51-+0800.xcresult`。第一次未带本地 source package 的 `p1.sh ui` 受缓存/网络权限阻塞，未作为通过证据。
- 未伪造或注入 `SYNORA_P2_IME_EVIDENCE`、`SYNORA_P2_VOICEOVER_EVIDENCE`、`SYNORA_P2_INSTRUMENTS_EVIDENCE`；这些字段保持 `UNVERIFIED`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| `zsh -n script/p2.sh` | 1 | `PASS` |
| P2 stage unit | 1 | `PASS`，58 个测试、0 失败 |
| P2 stage integration | 1 | `PASS`，26 个测试、0 失败 |
| SIGKILL recovery probe | 1 | `PASS`，writer exit `137` 后 `P2_RECOVERY_CHECK=PASS`，`operations=1380`、`revision=1380` |
| 100k+ text/200 media performance probe | 1 | `PASS`，UTF-16 长度 `150000`、媒体 `200`，图片/PDF/音频 `67/67/66`；报告明确 `modelOnly=true` |
| `SynoraWikiUITests` 壳层旅程 | 1 | `PASS`，6 个测试、0 失败、102.965 秒；不覆盖真实 IME/VoiceOver/媒体编辑 |
| `git diff --check` | 1 | `PASS` |
| P2 stage 出口 | 1 | `CHANGES_REQUIRED`：UI/visual 未由 stage 脚本执行，真实 IME/VoiceOver/keystroke-to-paint 均 `UNVERIFIED` |

性能探针报告位于 `/private/tmp/synora-p2-stage-final/performance-report.json`：编辑模型 p95 `0.177 ms`、重开 `3.466 ms`、保存 `13363.126 ms`。这些是领域/存储探针数字，不是 keystroke-to-paint 或首屏 UI 预算的通过证据。

## 5. 用户如何进行手工测试

先用本地依赖配置运行 `SYNORA_P2_GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-local-gitconfig ./script/p2.sh unit`，再运行 `recovery` 和 `performance`；检查 `stage-status.json` 是否保留 `CHANGES_REQUIRED`。在试验宿主中创建记录后使用 `AutosaveCoordinator`，修改内容并等待 flush，重开 `ProductStore` 检查记录、文档、revision、history、undo/redo 和冲突草稿；套用模板时确认块 ID 被复制且已有内容不会被静默替换，修改 metadata 后重开并撤销/重做。使用 `RecordExportService` 导出 Markdown、HTML、PDF 和原始包，再导入并比较记录/块 ID、未知字段、元数据和资产字节哈希；篡改包内资产或 payload 应拒绝恢复结构。现有壳层可重复运行 6 个 XCUITest，但真实中文输入法、VoiceOver、长文滚动、200 媒体和 attachment 全流程仍需在应用界面中手工完成。

## 6. 风险预估与规避

- **阶段出口未满足（极高）**：`stage-status.json` 有意将真实 IME、VoiceOver 和 keystroke-to-paint 标为 `UNVERIFIED`；没有实际系统输入、辅助功能读屏和 Instruments/signpost 证据就不提升为 P2 `PASS`。
- **性能数字被误读（高）**：探针通过 Swift 领域编辑和 SQLite 保存验证规模与稳定性，`editP95Milliseconds` 不是屏幕绘制延迟，`saveMilliseconds` 也不等于首屏打开；完整 100k/200 媒体 UI 预算留在质量门。
- **崩溃/文件系统覆盖不足（高）**：SIGKILL 后的 SQLite 完整性和 operation replay 已通过；磁盘满、权限撤销、损坏媒体、原生 AV/PDF/Quick Look 宿主和长期运行仍未注入，保持 `UNVERIFIED`。
- **壳层接线不足（高）**：6/6 XCUITest 只证明现有导航、搜索、主题、工具栏旅程；编辑器、媒体、模板、导出尚未接入这组 UI 测试。
- **并发改动误提交（高）**：本轮提交 `3b8c9f5` 只包含 4 个 P2 文件，7 个既有 App/Xcode 工作树差异保持未暂存。

## 7. 下轮建议

在不改变领域合同的前提下接入应用壳层，先完成真实 TextKit 2 中文 IME 和 VoiceOver 旅程，再以 Instruments 记录 100k 字/200 媒体的 keystroke-to-paint、首屏和滚动；补跑 attachment 导入/替换/导出、磁盘满/权限撤销/损坏媒体/进程终止矩阵。所有门通过后再更新阶段状态；当前不推送、不宣称 P2 完成。

## 8. 本轮任务大白话讲解

用户现在可以在真实本地数据库里自动保存内容，关闭再打开还能继续撤销、重做和查看历史；模板会复制出新的块身份，元数据和正文一起保存；Markdown、HTML、PDF 和原始包可以导出，导入时会检查结构和附件有没有被篡改。我们还准备了一个会被强制杀掉的恢复程序和一个 100k 字/200 媒体规模探针。现有壳层的 6 条导航类 UI 测试通过，但真正的中文输入法、读屏、屏幕绘制延迟以及媒体编辑界面还没有实机证据，所以 P2 阶段仍需补验。

# 2026-09-07 18:36 · P 2 · 第 9 步，对应开发计划中的 9 步 · P2-08 媒体块与链接卡片

## 1. 本轮任务列表

- 在 P2-07 资产引用和 P2-03 块树之上增加 image、collage、gallery、video、audio、PDF、file 和 link block；校验媒体内容基数、重复资产 ID、裁剪范围和链接 URL。
- 为媒体 block 提供放置、排序、替换、说明、裁剪和布局命令；EditorKit 入口沿用 `EditorSession` 的一次撤销提交。
- 增加 `MediaPreviewCoordinator`，按可见项和有界预取调度预览，并用 block ID、revision、取消状态拒绝迟到结果写回。
- 复用 `AssetStore` 原件路径，提供 AVFoundation 播放器、PDFKit 文档和 Quick Look 原生预览适配器；预览失败保留原件和资产引用。
- 增加受控链接预览：只有显式允许网络时才请求，限制 1 MB 响应、检查 HTTP 状态并解析标准 HTML title/description；离线或失败时继续显示已保存卡片。
- 不修改另一会话的 7 个应用/Xcode 未提交文件，不把本轮 focused 结果扩展为真实 UI、媒体运行时、IME/VoiceOver 或性能验收。

## 2. 本轮任务完成度

`100%`：提交 `cfff70e`、`c941104` 和 `6248a45` 已完成 P2-08 代码范围内的媒体 block 组合、原生预览协调和受控链接预览。应用壳层尚未接线，真实 AV/PDF/Quick Look、网络策略、IME/VoiceOver、长文/媒体性能及 P2 阶段出口仍未验收。

## 3. 测试方式

- 使用独立 SwiftPM scratch、临时 Swift/Clang module cache 和临时 Git 配置运行 `Packages/SynoraCore` focused 回归；未修改全局代理、依赖声明或真实用户库。
- 最终命令：
  - `CLANG_MODULE_CACHE_PATH=/private/tmp/synora-p2-08-domain-env/clang SWIFT_MODULECACHE_PATH=/private/tmp/synora-p2-08-domain-env/swift GIT_CONFIG_GLOBAL=/private/tmp/synora-p2-08-domain-env/gitconfig swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path Packages/SynoraCore/.build -Xswiftc -warnings-as-errors --filter SynoraDomainTests --filter SynoraEditorKitTests --filter SynoraAssetsTests --filter SynoraStoreTests --filter SynoraExportTests`
- Domain/EditorKit 测试覆盖媒体内容校验、布局与 placement 操作、替换/说明/裁剪、链接卡片和撤销；Assets 测试覆盖预览协调、有限预取、取消/迟到结果、原生资产错误、离线链接和 stub HTML metadata；Store/Export 作为直接受影响回归一并运行。
- 未在受限环境注入真实权限撤销、磁盘满、进程 SIGKILL、AV/PDF/Quick Look 宿主和真实网络，故这些门保持 `UNVERIFIED`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain、EditorKit、Assets、Store、Export focused 回归 | 1 | `PASS`，51 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

## 5. 用户如何进行手工测试

本轮没有修改应用壳层，因此没有新增可启动的 UI 手工验收入口。接入本轮 API 后，可创建 image/collage/gallery、video、audio、PDF、file 和 link block，检查 placement 排序、替换、说明、裁剪、布局切换及一次撤销；对可见媒体调用 `MediaPreviewCoordinator`，在 revision 变化或取消后确认迟到结果不回写。对本地视频/音频、PDF 和文件分别调用 AVPlayer、PDFKit 和 Quick Look 入口。链接卡片在 `allowsNetwork: false` 时应保留已保存标题/摘要，允许网络时用受控 transport 检查 metadata 更新；所有真实宿主与网络行为仍需 P2-12 质量门验证。

## 6. 风险预估与规避

- **媒体结构错误（高）**：创建和设置入口统一校验允许的内容基数、重复资产 ID、有限裁剪矩形和受支持的 HTTP/HTTPS URL；非法操作原子拒绝。
- **迟到预览污染文档（高）**：协调器按 block ID、revision 和完整请求匹配结果，并在取消时移除活动请求；缺失/不可读原件只返回失败状态，不删除原件。
- **链接网络与内容不受控（高）**：远程请求必须显式传入 `allowsNetwork`，响应上限为 1 MB，非成功 HTTP 响应和无 metadata 页面失败；失败保持本地保存卡片。
- **原生框架和长期稳定性（高）**：AVFoundation、PDFKit、Quick Look 适配器只提供可复用入口，尚未证明真实窗口、权限、媒体损坏、长文和 200 媒体性能；这些项目保留为 `UNVERIFIED`。
- **并发改动误提交（高）**：本轮提交只包含 Domain、EditorKit、Assets、对应 focused 测试和阶段日志；7 个应用/Xcode 文件继续保持未暂存。

## 7. 下轮建议

在已完成的 P2-09A 真实事务基础上进入 P2-09B：实现自动保存、跨进程 crash recovery、history 体验和编辑持久性收口；继续把媒体引用与原件恢复纳入真实事务，不用内存 mock 代替验收。

## 8. 本轮任务大白话讲解

用户现在可以把图片、视频、音频、PDF、文件和链接放进内容块，还能排列图片、写说明、裁剪和替换。系统优先从本地原件生成预览，异步任务如果已经过期或被取消就不会覆盖新内容；链接只有在明确允许联网时才抓取标题和摘要，断网或失败仍显示用户之前保存的卡片。原件不会因为预览失败被删除，真正的界面接线和恢复/性能验收留给后续质量门。

# 2026-09-07 17:55 · P 2 · 第 8 步，对应开发计划中的 8 步 · P2-07 附件管线

## 1. 本轮任务列表

- 沿用 P2-06 的文件粘贴 URL 入口，统一 `AssetImportSource`、单文件和批量导入；原件先复制到 staging，再流式计算 SHA-256、同步落盘并在同卷按内容哈希原子移动。
- 为内容仓增加并发安全的内容寻址去重、取消/来源/目标错误边界、staging 清理和按有效资产哈希恢复对账；恢复保留有效引用并删除未提交孤儿文件。
- 使用 ImageIO/PDFKit 生成按需 PNG 缩略图，提供 Quick Look 和 AVFoundation 的异步缩略图入口，并以哈希缓存派生结果；导入仍串行执行以限制内存峰值。
- 为媒体 block 增加 `AssetPlacement` 的放置、移除和排序读出；EditorSession 命令与 ProductStore 的资产+文档事务保持一次撤销和一次数据库提交。
- 不修改另一会话的 7 个应用/Xcode 未提交文件，不进入 P2-08 媒体块视图或 P2-12 真实 IME/VoiceOver/性能门。

## 2. 本轮任务完成度

`100%`：P2-07 代码范围已完成。导入、内容寻址、派生缩略图、恢复对账、媒体 placement、编辑撤销和数据库原子提交均有 focused 证据；P2 阶段仍未整体验收。

## 3. 测试方式

- 使用本地 GRDB/SQLite checkout 的临时 URL 镜像、独立 SwiftPM scratch、临时 Swift/Clang module cache 和隔离临时文件库；未修改全局代理、依赖声明或真实用户库。
- focused 命令：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraAssetsTests -Xswiftc -warnings-as-errors`
  - 同命令分别运行 `SynoraDomainTests`、`SynoraEditorKitTests`、`SynoraStoreTests` 和 `SynoraExportTests`。
- 资产测试覆盖重复内容、同名不同内容、批量/文件源入口、取消、缺失源、目录源、staging 恢复、孤儿内容清理、ImageIO 缩略图缓存；Store 测试覆盖资产 placement 与文档同事务提交、幂等重放、撤销保留历史资产和资产 ID 冲突。
- 每次修正后重跑受影响 focused 测试；最终执行 `git diff --check`。未把未运行的磁盘满、权限撤销和 SIGKILL 恢复写成通过证据。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Assets focused | 1 | `PASS`，4 个测试、0 失败 |
| Domain focused | 1 | `PASS`，14 个测试、0 失败 |
| EditorKit focused | 1 | `PASS`，15 个测试、0 失败 |
| Store focused | 1 | `PASS`，9 个测试、0 失败 |
| Export focused 回归 | 1 | `PASS`，2 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

## 5. 用户如何进行手工测试

最终产物：`/private/tmp/synora-p2-07-final.XGrwgO/`。

本轮没有修改应用壳层，因此没有新增可启动的 UI 验收入口。可在试验宿主中把文件选择、拖放和 P2-06 文件粘贴的 URL 送入 `AssetImportSource.file`，观察进度、内容哈希和 staging 清空；导入同内容的不同文件名只保留一个原件路径，同名不同内容保留两个哈希路径。对图片调用 `preview(for:)` 检查尺寸和 PNG 缩略图，对文件调用异步 `thumbnailURL(for:)` 检查 Quick Look 结果。创建 image/gallery block 后用 `EditorSession.placeAsset`、`removeAsset` 并撤销；把包含 placement 的文档和 `Asset` 一起交给 `ProductStore.save(asset:record:document:expectedRevision:)`，重开后检查 placement、哈希和 revision。

## 6. 风险预估与规避

- **文件系统/SQLite 无共享事务（极高）**：原件先同卷原子发布，ProductStore 再把 asset metadata 与 placement 文档写入同一个 operation/save 事务；失败路径只清理 staging，恢复按数据库有效资产哈希保留有效原件并删除未提交孤儿。
- **重复导入与并发竞态（高）**：目标以 SHA-256 路径寻址；并发移动遇到已存在目标时视为成功，批量入口串行限制内存，后续只有测得吞吐不足才扩并发。
- **媒体损坏/预览失败（高）**：原件 hash/字节数仍可提交，ImageIO/PDFKit/Quick Look/AVFoundation 失败只返回无缩略图状态，不删除原件；P2-08 再接可操作的媒体视图错误反馈。
- **取消、权限和磁盘故障（高）**：取消、源不可读和目标不可用分别映射错误且 defer 清理 staging；本轮未在受限环境注入权限撤销、磁盘满或进程终止，保留为 P2-12/恢复门的 `UNVERIFIED`。
- **并发改动误提交（高）**：提交前只暂存本轮 Assets/Domain/EditorKit/Store、对应测试和阶段日志；7 个应用/Xcode 文件继续保持未暂存。

## 7. 下轮建议

按计划进入第 9 步 P2-08：在现有 placement 与预览入口之上实现图片/拼贴/画廊、视频、音频、PDF、文件和链接卡片；继续保持原件与派生层分离、迟到任务不能写回已删除或替换的 block。

## 8. 本轮任务大白话讲解

用户选择、拖放或粘贴文件后，系统会先复制一份到临时区，一边复制一边算哈希；复制成功才把原件放到按哈希命名的仓库里，同一内容不会重复占磁盘。图片和 PDF 可以按需得到缩略图，其他文件交给 Quick Look，失败也不影响原件保留。媒体块只保存对原件的 placement，资产与文档可以在一次数据库事务中提交；撤销会去掉 placement，但历史仍能找到原件。

# 2026-09-07 17:29 · P 2 · 第 7 步，对应开发计划中的 7 步 · P2-06 编辑工具

## 1. 本轮任务列表

- 读取 `SynoraDomain`、`SynoraEditorKit` 的编辑工具调用链，限定本轮为选区格式、查找替换、系统拼写入口和安全粘贴；不触碰另一会话的 7 个应用/Xcode 文件。
- 为 block 增加可持久化的 UTF-16 行内属性，支持粗体、斜体、删除线、行内代码和链接；格式栏状态只在有效非空选区显示，并支持跨块和混合选区的一次撤销。
- 增加中文安全的查找、全部替换和大小写/整词选项；全部替换沿用 `TextStorageAdapter`，保持块结构与富文本范围并作为一个撤销动作提交。
- 复用 macOS `NSSpellChecker` 作为拼写入口；在非 AppKit 环境保留空结果适配，未把伪造词典当作产品能力。
- 增加结构化、富文本、HTML、纯文本和文件粘贴准备；结构和 URL 先校验，HTML 去除脚本/外部资源标签，文件 URL 保留给后续附件管线。
- 增加 EditorKit focused 回归，并复核 Domain/Store/Export 直接受影响的测试。

## 2. 本轮任务完成度

`100%`：P2-06 计划范围内的选区格式栏状态、富文本属性、中文查找替换、系统拼写入口、安全粘贴准备和整组撤销已完成。应用壳层未接入本轮工具栏/系统面板，P2 阶段仍未整体验收。

## 3. 测试方式

- 使用本地 GRDB/SQLite checkout 的临时 URL 镜像、临时 `GIT_CONFIG_GLOBAL`（含 `protocol.file.allow`）、独立 SwiftPM scratch 和 Clang/Swift module cache 运行 focused 测试；未修改全局代理、依赖声明或用户库。
- 最终命令均写入 `/private/tmp/synora-p2-06-final.aEgZiG/`：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraEditorKitTests -Xswiftc -warnings-as-errors`
  - 同命令分别运行 `SynoraDomainTests`、`SynoraStoreTests` 和 `SynoraExportTests`。
- 初次 SwiftPM 运行因受限环境禁止 file submodule transport；临时 Git 配置修复后，首次编译只发现格式栏状态的 `Set.reduce` 调用写法错误，修正后重跑。产品代码未通过放宽测试或跳过校验来掩盖失败。
- 运行 `git diff --check`。严格 `swift-format lint` 对完整既有文件报告已有格式违规；未自动重排共享文件，故不把该基线检查记为本轮通过。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| EditorKit focused（最终 artifact） | 1 | `PASS`，14 个测试、0 失败 |
| Domain focused（最终 artifact） | 1 | `PASS`，13 个测试、0 失败 |
| Store focused 回归（最终 artifact） | 1 | `PASS`，8 个测试、0 失败 |
| Export focused 回归（最终 artifact） | 1 | `PASS`，2 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |
| `xcrun swift-format lint --strict`（既有文件基线） | 1 | `BASELINE_RED`，已有格式违规；未自动重排 |

最终产物：`/private/tmp/synora-p2-06-final.aEgZiG/`。

## 5. 用户如何进行手工测试

本轮没有修改应用壳层，因此没有新增可启动的 UI 手工入口。接入 EditorKit API 后，可选中同一块或跨块文字，确认格式栏状态、粗体/斜体/删除线/行内代码/链接切换和混合选区；用系统查找面板输入中文并全部替换，确认一次撤销恢复；通过系统拼写面板检查英文输入；分别粘贴 Synora 结构、受支持富文本、含脚本的 HTML、纯文本和文件 URL，确认结构/URL 校验、脚本文本被丢弃、文件 URL 交给附件管线且不自动联网。

## 6. 风险预估与规避

- **范围错位（高）**：所有行内属性和查找范围使用 UTF-16；格式和粘贴入口沿用组合字符边界校验，文本替换同步裁剪/移动属性。
- **混合选区误切换（高）**：格式栏只报告所有选中片段共同拥有的样式；只有全选区均已标记时才执行移除，部分标记统一为一次添加。
- **重复撤销（高）**：格式、全部替换和富文本粘贴先构造完整 `BlockDocument`，最后只调用一次 `EditorSession.commit`。
- **恶意剪贴板（高）**：结构化块 ID、父子关系、内容类型、行内范围和链接 URL 先校验；HTML 在转纯文本前移除脚本、样式、iframe、object 和 embed 内容。
- **并发改动误提交（高）**：提交前只暂存 Domain、EditorKit、Store 模板复制、对应测试和阶段日志；7 个应用/Xcode 文件继续保持未暂存。
- **阶段越界（中）**：本轮未接入真实 AppKit 工具栏、查找面板和附件导入，不把 focused 结果当 UI、IME、VoiceOver 或 P2 PASS 证据。

## 7. 下轮建议

按计划进入第 8 步 P2-07：复用本轮文件粘贴准备和稳定 block ID，接入 staging、流式 SHA-256、同卷原子移动、取消与恢复对账；不在编辑器里直接写入未验证的文件路径。

## 8. 本轮任务大白话讲解

用户现在可以选中一段文字给它加粗、斜体、删除线、行内代码或链接，跨多个块选择时也不会把块结构打散。查找替换能正确处理中文，并把整次替换作为一个动作撤销。粘贴内容会先过结构、范围和链接检查，HTML 里的脚本不会进入文档；文件只被识别并交给下一步附件流程，不会偷偷下载或执行。

# 2026-09-07 17:06 · P 2 · 第 6 步，对应开发计划中的 6 步 · P2-05 输入命令与引用

## 1. 本轮任务列表

- 读取 `SynoraDomain`、`SynoraEditorKit` 的输入命令调用链，限定本轮为 Markdown 快捷输入、斜杠菜单和本地引用；不触碰另一会话的 7 个应用/Xcode 文件。
- 为快捷输入增加代码块和 marked text 禁用边界，补齐任务 `[x]`/`[ ]` 状态识别，并让命令执行经过同一撤销链。
- 为斜杠菜单增加中英文匹配、空候选、上下移动、回车选择和 Escape 取消；执行命令时只清除触发文本，不吞掉无关输入。
- 为 `@`/`[[` 引用增加稳定目标 ID、候选过滤、语法标记、目标解析、缺失目标处理、光标命中和 backlinks；插入引用后支持一次撤销恢复。
- 增加 EditorKit focused 回归，并复核 Domain/Store/Export 直接受影响的测试。

## 2. 本轮任务完成度

`100%`：P2-05 计划范围内的 Markdown 快捷输入边界、斜杠菜单键盘状态、任务状态、稳定引用和撤销行为已完成。应用壳层未接入本轮输入 UI，P2 阶段仍未整体验收。

## 3. 测试方式

- 使用本地 GRDB/SQLite checkout 的临时 URL 镜像、独立 SwiftPM scratch、Clang/Swift module cache 运行 focused 测试；未修改全局代理、依赖声明或用户库。
- 最终运行：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraDomainTests -Xswiftc -warnings-as-errors`
  - 同命令分别运行 `SynoraEditorKitTests`、`SynoraStoreTests` 和 `SynoraExportTests`。
- 首次实现后的 focused 失败只涉及测试断言与实际 API 错误类型/调用方式不一致；修正断言后重跑，未以放宽产品行为通过。
- 运行 `git diff --check`，并检查暂存文件名只包含本轮 Domain、EditorKit、EditorKit 测试和阶段日志。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain focused（本地镜像，最终） | 1 | `PASS`，13 个测试、0 失败 |
| EditorKit focused（本地镜像，最终） | 1 | `PASS`，11 个测试、0 失败 |
| Store focused 回归（本地镜像，最终） | 1 | `PASS`，8 个测试、0 失败 |
| Export focused 回归（本地镜像，最终） | 1 | `PASS`，2 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

最终产物：`/private/tmp/synora-p2-05-regression.ZWABt1`。

## 5. 用户如何进行手工测试

本轮没有修改应用壳层，因此没有新增可启动的 UI 手工入口。接入本轮 EditorKit API 后，可在普通段落输入 `#`、`- [ ]` 和 `/` 验证快捷转换；在代码块或 marked text 中确认快捷输入保持原文；在斜杠菜单中用中英文查询、上下键、回车和 Escape；输入 `@`/`[[` 后选择本地记录，改名后引用仍按稳定 ID 解析，缺失目标显示为不可解析且不误绑同名对象；插入后一次撤销应恢复原文。

## 6. 风险预估与规避

- **输入吞字（高）**：快捷转换仅处理明确前缀，代码块和 marked text 直接禁用；Slash 命令清除触发文本前先校验当前块文本。
- **菜单键盘状态错乱（中）**：空候选保持无选中项，循环移动只在候选非空时执行，回车和 Escape 都关闭菜单并返回明确动作。
- **引用漂移或误解析（高）**：token 只写入 UUID，不依赖标题；解析保留语法和 UTF-16 range，目标缺失返回 `nil`，backlinks 只扫描当前文档集合。
- **并发改动误提交（高）**：提交前只暂存 Domain、EditorKit、对应测试和阶段日志；7 个应用/Xcode 文件继续保持未暂存。
- **阶段越界（高）**：未实现真实输入法、VoiceOver、应用壳层菜单和远程目标查询；本轮 focused 结果不当作 UI 或 P2 PASS 证据。

## 7. 下轮建议

按计划进入第 7 步 P2-06：复用 `TextStorageAdapter` 和 `EditorSession`，补齐选区格式栏、查找替换、拼写边界与复制粘贴交互；继续保持一次用户动作对应一次撤销。

## 8. 本轮任务大白话讲解

用户现在可以把常用的 Markdown 前缀直接变成对应块，也能用 `/` 菜单按中英文找命令；代码块和正在输入的 marked text 不会被半截转换。`@` 和 `[[` 引用保存的是目标 ID，目标改名不会让链接失效，目标不存在时也不会错误地连到同名内容；插入和撤销仍是一进一出。

# 2026-09-07 16:52 · P 2 · 第 5 步，对应开发计划中的 5 步 · P2-04 高级 blocks

## 1. 本轮任务列表

- 读取 `SynoraDomain`、`SynoraEditorKit` 的现有调用链，限定本轮为表格、折叠块、callout 和嵌套规则；不触碰另一会话的 7 个应用文件。
- 将 table 从可嵌套父块修正为行/单元格结构，补齐矩形化、单元格文本编辑、增删行列、方向键/Tab 导航和复制所需的结构保留。
- 为 toggle 增加收起状态、嵌套内容可见性和编辑会话焦点保存/恢复；折叠时隐藏子树文本范围，展开时按稳定 Block ID 恢复焦点。
- 为 callout 增加样式、正文编辑和可嵌套子块支持；高级命令统一经过 `EditorSession` 的撤销/重做栈。
- 增加 Domain/EditorKit focused 回归，并检查 Store/Export 直接受影响的编码、持久化和导出测试。

## 2. 本轮任务完成度

`100%`：P2-04 计划范围内的领域模型、表格编辑/导航、toggle 收起与焦点恢复、callout 样式和合法嵌套已完成。应用壳层未接入本轮高级块视图，P2-05 及后续步骤未启动，P2 阶段仍未整体验收。

## 3. 测试方式

- 使用本地 GRDB/SQLite checkout 的临时 `GIT_CONFIG_GLOBAL` URL 映射、独立 SwiftPM scratch、Clang/Swift module cache 运行 focused 测试；未修改全局代理、依赖声明或用户库。
- 最终运行：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraDomainTests -Xswiftc -warnings-as-errors`
  - 同命令分别运行 `SynoraEditorKitTests`、`SynoraStoreTests` 和 `SynoraExportTests`。
- 首次无本地镜像的 SwiftPM 尝试因受限环境无法写全局 module cache、解析远程依赖失败；随后只切换到临时本地镜像重跑。Domain 测试初次编译发现新增断言直接调用 mutating 成员，EditorKit 初次运行发现测试对非法 callout 命令的错误类型预期不符；均只修正测试断言后重跑。
- 运行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain focused（本地镜像，最终） | 1 | `PASS`，13 个测试、0 失败 |
| EditorKit focused（本地镜像，最终） | 1 | `PASS`，9 个测试、0 失败 |
| Store focused 回归（本地镜像，最终） | 1 | `PASS`，8 个测试、0 失败 |
| Export focused 回归（本地镜像，最终） | 1 | `PASS`，2 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

最终产物：`/private/tmp/synora-p2-04-final.hMZPNg`。

## 5. 用户如何进行手工测试

本轮没有修改应用壳层，因此没有新增可启动的 UI 手工入口。接入本轮领域 API 后，可在试验宿主中创建 2×2 表格，编辑首末单元格、增删行列并用方向键/Tab 移动；创建带子块的 toggle，收起时确认子块不进入连续文本范围、展开后原 Block ID 和焦点恢复；创建 callout、切换 warning/info 样式并编辑正文。复制整张表或整个容器后，应看到结构保留且粘贴块 ID 重映射。

## 6. 风险预估与规避

- **表格形状与导航错位（高）**：`TableContent` 初始化/解码统一补齐短行；所有单元格操作先校验坐标，方向键和 Tab 只返回合法位置，末单元格 Tab 才追加一行。
- **折叠后选区丢失（高）**：连续文本适配器跳过 collapsed toggle 子树；编辑会话保存目标 Block ID 和 UTF-16 偏移，展开时目标仍存在才恢复，否则保留容器焦点。
- **非法嵌套（高）**：统一由 `BlockType.acceptsChildren` 和 `BlockDocument.validate()` 拒绝 table/媒体下的子块，同时允许 task、toggle、callout 的合法子块；失败保持原文档不变。
- **并发改动误提交（高）**：提交前只暂存 Domain、EditorKit、两份 focused 测试和本日志；7 个应用文件及 Xcode 工程差异继续未暂存。
- **阶段越界（中）**：未实现复杂块的应用视图桥接、真实 IME/VoiceOver、附件全流程和长文性能门，不把本轮 focused 结果当 P2 PASS。

## 7. 下轮建议

按计划进入第 6 步 P2-05：复用现有 Markdown/Slash 命令基础，补齐代码块和 marked text 禁用、斜杠菜单键盘行为以及本地 `@`/`[[` 引用；继续保持候选不吞输入和一次撤销恢复。

## 8. 本轮任务大白话讲解

用户现在可以在内容树里使用真正的表格：每个格子有自己的文字，新增或删除行列不会把其他格子的内容挤乱，按方向键或 Tab 能移动到下一个格子。折叠块收起时，里面的文字暂时从编辑连续文本里隐藏，但它们的身份和内容仍留在文档中；重新展开会回到原来的位置。callout 有可保存的提示样式和正文，也能装合法的子块。所有这些动作仍通过同一个文档和撤销链提交，保存层能看到完整结构变化。
# 2026-09-07 16:05 · P 2 · 第 4 步，对应开发计划中的 4 步 · P2-03 基础块

## 1. 本轮任务列表

- 读取 `SynoraDomain`、`SynoraEditorKit` 的直接调用链和已有 focused 测试，限定改动在 P2-03 基础块行为。
- 补齐段落、标题、无序/有序列表、任务、引用、代码、分隔线的创建、文本编辑、类型转换和任务状态读出。
- 补齐回车拆分、空列表退出、退格删除/相邻块合并、分隔线后新建段落，以及组合字符边界拒绝。
- 补齐块树复制/粘贴，保留子树结构并为粘贴块生成新 ID；移动到自身后代时原子拒绝。
- 修复 order key 间隔耗尽时按请求位置只重排目标兄弟组；拆分保留原块内容。
- 增加 Domain/EditorKit focused 回归，并检查 Store/Export 直接受影响的测试。

## 2. 本轮任务完成度

`100%`：P2-03 计划范围内的基础块领域操作、编辑会话行为、复制粘贴和 focused 测试已完成。未修改另一会话的 7 个应用文件，也未进入 P2-04 及后续步骤。

## 3. 测试方式

- 使用本地已存在的 GRDB/SQLite checkout 建立 `/private/tmp` 临时 bare mirror，通过临时 Git URL 映射和临时 Clang/Swift module cache 运行 SwiftPM。
- 运行：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraDomainTests -Xswiftc -warnings-as-errors`
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraEditorKitTests -Xswiftc -warnings-as-errors`
  - 同样方式运行 `SynoraStoreTests` 和 `SynoraExportTests` 回归。
- 每次修正后重跑受影响 focused 测试；最终运行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain focused（首次实现后） | 1 | 失败：测试用例将两次独立生成的随机 UUID 当作同一 missing sibling，产品代码未因该断言失败修改 |
| Domain focused（修正断言后） | 1 | `PASS`，11 个测试、0 失败 |
| EditorKit focused（首次实现后） | 1 | `PASS`，7 个测试、0 失败 |
| Domain focused（最终） | 1 | `PASS`，12 个测试、0 失败 |
| EditorKit focused（最终） | 1 | `PASS`，8 个测试、0 失败 |
| Store focused 回归（最终） | 1 | `PASS`，8 个测试、0 失败 |
| Export focused 回归（最终） | 1 | `PASS`，2 个测试、0 失败 |
| Domain/EditorKit/Store/Export focused（本轮复核） | 1 | `PASS`，分别为 12/8/8/2 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

本轮复核产物：`/private/tmp/synora-p2-03-current.7b8Atg`；此前最终 focused 产物：`/private/tmp/synora-p2-03-focused-final2.hF9N4C`；此前 Store/Export 回归产物：`/private/tmp/synora-p2-03-regression-final.Av6QQk`。

## 5. 用户如何进行手工测试

本轮没有改动应用壳层，因此没有新增 UI 手工验收入口。接入本轮领域 API 后，可在编辑器中逐项验证：创建七种基础块；在标题、列表、任务、引用和代码中回车拆分；空列表回车退出为段落；块首退格合并；分隔线回车生成段落；任务切换时旁白读出 `Unchecked/Completed task`；复制带子项的列表后粘贴，原块 ID 与子树不互相影响。

## 6. 风险预估与规避

- **身份和树结构破坏（高）**：复制粘贴统一重映射 ID；移动到自身后代、非法父节点、错误 sibling 和重复剪贴板 ID 均原子拒绝。
- **Unicode 范围错位（高）**：回车/退格沿用 UTF-16 范围和组合字符边界校验；组合字符内部退格有回归断言。
- **列表顺序漂移（中）**：order key 无间隔时按目标插入位置重排目标兄弟组，其他父组的 order key 保持不变。
- **并发改动误提交（高）**：提交前仅暂存本步骤的 Domain、EditorKit、对应测试和阶段日志；7 个既有应用差异继续保持未暂存。
- **阶段越界（高）**：未实现表格/折叠/callout、快捷输入、附件和媒体；P2-04 之后步骤保持未启动。

## 7. 下轮建议

按计划进入第 5 步 P2-04：只处理表格、折叠和 callout 的合法嵌套及键盘行为；继续沿用本日志顶部追加、focused 验证、差异检查、单独提交的节奏。

## 8. 本轮任务大白话讲解

用户现在可以把一条记录拆成真正有意义的基础块：标题有层级，列表能连续编号，任务有完成状态，引用和代码保留自己的类型，分隔线不会吞掉输入。按回车会把内容拆成两个可撤销的块，空列表会退出成普通段落，块首退格会和上一块合并；复制列表时会复制整棵子树但生成新的身份。底层仍把这些变化作为同一个文档模型操作，保存层看到的是结构变化，不是只改了一段显示文本。

# 2026-09-07 14:17 · P 2 · 第 3 步，对应开发计划中的 3 步 · P2-02 连续编辑核心

## 1. 本轮任务列表

- 增加 `SynoraEditorKit` library/test target，让编辑器核心直接依赖 P2-01 的 `BlockDocument`。
- 实现 `TextStorageAdapter`：把根块展平为连续文本，保留每个块的 UTF-16 `NSRange` 映射，并把单块替换写回文档。
- 在编辑入口校验范围边界和组合字符边界；标记文本暂不提交模型变更，避免 IME 中间态破坏领域数据。
- 增加确定性的 Markdown 快捷输入解析、编辑命令和错误类型。
- 提供基于 TextKit 2 的 `SynoraTextView` AppKit 适配器，启用 delegate、撤销、可编辑/可选中和基础布局；不提前实现 P2-03 的块级回车和列表行为。

## 2. 本轮任务完成度

`100%`：提交 `f0649e4` 已完成 P2-02 计划范围内的文本存储适配、UTF-16 选区保护和 TextKit 2 壳层基础。跨块拆分、块类型转换、IME/VoiceOver 和性能质量门留给后续步骤，P2 阶段仍未整体验收。

## 3. 测试方式

- 从提交 `f0649e4` 导出干净的 `Packages/SynoraCore` 快照，在 `/private/tmp` 使用独立 SwiftPM scratch、Clang/Swift module cache 和临时 Git 配置。
- 首次只映射 GRDB 主仓库运行以下 focused 命令时，SwiftPM 因同一 GRDB `SQLiteCustom/src` 子模块传输限制退出；没有改动编辑器代码或依赖声明：
  - `swift test --disable-sandbox --package-path <临时目录>/step3/Packages/SynoraCore --scratch-path <临时目录>/step3/build --filter SynoraEditorKitTests -Xswiftc -warnings-as-errors`
- 随后补齐 GRDB 与 `SQLiteLib` 的本地 `insteadOf` 映射并重跑；最终产物保存在 `/private/tmp/synora-p2-history-logs-final.ECjR2N/step3/result.log`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| EditorKit focused（仅 GRDB 映射） | 1 | 失败：`SQLiteCustom/src` 的 HTTPS 子模块传输被环境拒绝，属测试环境问题 |
| EditorKit focused（GRDB + SQLiteLib 本地映射） | 1 | `PASS`，2 个测试、0 失败；覆盖 UTF-16 映射、单块替换、组合字符拒绝和 Markdown 快捷输入 |
| `git show --check f0649e4` | 1 | `PASS`，历史实现提交无空白错误 |

## 5. 用户如何进行手工测试

本轮没有把 `SynoraTextView` 接入应用壳层，因此没有新增 UI 手工验收入口。可在 SwiftPM 试验代码中用含中文和 emoji 的文档检查连续文本与块范围，替换一个 UTF-16 选区并确认只改目标块；在 emoji 的代理项中间选区应被拒绝。宿主 AppKit 运行时可嵌入 `SynoraTextView` 输入文本并观察 delegate 回调，但 IME、VoiceOver 和长文本稳定性尚未验收。

## 6. 风险预估与规避

- **UTF-16/组合字符错位（高）**：范围计算统一基于 `NSString`，编辑前同时校验上下界和 composed-character boundary；focused 测试覆盖 emoji 代理项边界。
- **跨块编辑语义不足（中）**：当前适配器只把替换落在一个根块，跨块回车、删除、合并和块类型行为明确留给 P2-03，不把连续文本映射误报为完整块编辑。
- **IME 中间态丢失（高）**：`marked` 输入不直接写领域文档；真实 IME、VoiceOver 和 100k 字性能测试留给 P2-12 质量门。
- **运行时接线范围（中）**：本轮只增加可复用 AppKit 组件，没有修改另一会话的应用壳层文件，也未进行 Xcode UI 验收。

## 7. 下轮建议

按计划进入 P2-03，补齐段落、标题、列表、任务、引用、代码和分隔线的块级创建与键盘行为；继续复用本适配器的 UTF-16 边界保护。

## 8. 本轮任务大白话讲解

这一步把块文档接到真正的 macOS 文本编辑入口：编辑器看到的是连续文字，但每段文字仍能定位回自己的块，中文和 emoji 不会因为 UTF-16 位置算错而被切坏。用户界面尚未接线，回车拆块、列表退出和任务状态等能直接感知的行为留到下一步实现。

# 2026-09-07 14:10 · P 2 · 第 2 步，对应开发计划中的 2 步 · P2-09A 事务持久化基础

## 1. 本轮任务列表

- 在 P2-01 领域 schema 之上增加 `SynoraStore` 目标和 `ProductStore`，让记录与块文档进入真实 SQLite 存储。
- 建立 records、blocks、operations、snapshots 表和迁移，启用 WAL，并把记录、文档投影和操作日志放进同一写事务。
- 实现乐观 revision 校验、操作 ID 幂等重放、请求指纹、顺序号和前序 hash 链，避免重复请求或并发旧写覆盖数据。
- 提供记录创建/读取、文档读取、history、undo/redo、snapshot 和完整性校验入口。
- 只修改 Store 目标、实现和 focused 测试；P2-09A 是编辑持久性的基础，不扩展为自动保存、崩溃恢复或 UI 验收。

## 2. 本轮任务完成度

`100%`：提交 `358757f` 已完成执行安排中 P2-09A 的真实事务基础。完整 P2-09（自动保存、跨进程 crash recovery、历史 UI 和长期恢复体验）仍未完成，也未把本轮基础当作 P2 阶段通过。

## 3. 测试方式

- 从提交 `358757f` 导出干净的 `Packages/SynoraCore` 快照，在 `/private/tmp` 使用独立 SwiftPM scratch、Clang/Swift module cache、临时数据库和本地 Git URL 映射。
- 首次只映射 GRDB 主仓库运行以下 focused 命令时，SwiftPM 在更新 `SQLiteCustom/src` 子模块时被受限环境拒绝；没有改动 `Package.swift` 或 Store 代码：
  - `swift test --disable-sandbox --package-path <临时目录>/step2/Packages/SynoraCore --scratch-path <临时目录>/step2/build --filter SynoraStoreTests -Xswiftc -warnings-as-errors`
- 随后补齐 GRDB 与 `SQLiteLib` 的本地 `insteadOf` 映射并重跑；最终产物保存在 `/private/tmp/synora-p2-history-logs-final.ECjR2N/step2/result.log`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Store focused（仅 GRDB 映射） | 1 | 失败：`SQLiteCustom/src` 的 HTTPS 子模块传输被环境拒绝，属测试环境问题 |
| Store focused（GRDB + SQLiteLib 本地映射） | 1 | `PASS`，4 个测试、0 失败；覆盖原子保存/重开、旧 revision、幂等重放、undo/redo、snapshot 和 hash 校验 |
| `git show --check 358757f` | 1 | `PASS`，历史实现提交无空白错误 |

## 5. 用户如何进行手工测试

本轮没有接入应用壳层，因此没有新增 UI 手工入口。可在临时 SQLite 路径创建一条记录和块文档，保存后关闭并重新打开 `ProductStore`，检查记录、文档和 revision；重复同一 operation ID 应返回同一 receipt，使用旧 revision 应拒绝，随后验证 undo/redo、`history()`、`snapshot()` 和 `verifyIntegrity()`。

## 6. 风险预估与规避

- **记录与文档半保存（高）**：先校验 `record.id == document.recordID` 和文档树，再在同一个数据库写事务中追加 operation、更新记录 projection 和重写块 projection。
- **重复或旧写覆盖（高）**：以 expected revision 和 operation request hash 做并发/幂等边界；操作序列和 hash 链由 focused 测试验证。
- **崩溃恢复范围不足（高）**：当前仅提供事务日志、snapshot 和进程内 redo 栈；跨进程重放、自动保存调度和 crash recovery 留给 P2-09 后续工作。
- **依赖与验收边界（中）**：GRDB/SQLite 只通过临时本地镜像验证，未改全局代理或缓存；未运行 UI、IME、VoiceOver 或性能验收。

## 7. 下轮建议

回到计划依赖，记录并验证 P2-02 的 `TextStorageAdapter` 与 `SynoraTextView` 连续编辑核心；保存层继续作为真实持久化边界使用，不用内存 mock 代替。

## 8. 本轮任务大白话讲解

这一步让用户写下的记录和内容树真正“一起落盘”：保存时记录标题、块结构、版本号和操作凭证在同一个 SQLite 事务里完成，应用重新打开还能读回；重复请求不会产生第二次变化，旧版本写入会被挡住，撤销和重做也会留下新的审计操作。界面还没有接入这条链路，后续编辑器只需要把文档交给这个真实保存入口。

# 2026-09-07 14:04 · P 2 · 第 1 步，对应开发计划中的 1 步 · P2-01 Block schema

## 1. 本轮任务列表

- 对照 P2-01 和 P0 领域模型，确定记录、块、块类型和可扩展字段的 Codable 线格式。
- 增加段落、标题、列表、任务、引用、代码、分隔线及表格/媒体占位类型，并声明哪些类型允许子块。
- 实现 `BlockDocument` 的重复 ID、跨记录、缺失父节点、非法子块和环检测；按 `parentID`、`orderKey` 提供稳定的兄弟排序。
- 提供块插入、移动、删除、后代查询和 revision/legacy payload 默认值，保留未知字段以便前向兼容。
- 只修改 `SynoraDomain` 及其 focused 测试；不把当前步骤扩展为编辑器、应用壳层或阶段验收。

## 2. 本轮任务完成度

`100%`：提交 `c984547` 已完成 P2-01 的 schema、树不变量、排序和兼容性基础。order key 的间隔耗尽重排、连续编辑和持久化事务留给后续步骤，P2 阶段仍未整体验收。

## 3. 测试方式

- 从提交 `c984547` 导出干净的 `Packages/SynoraCore` 快照，在 `/private/tmp` 使用独立 SwiftPM scratch、Clang/Swift module cache 和临时 Git 配置。
- 首次只映射 GRDB 主仓库运行以下 focused 命令时，SwiftPM 能解析 GRDB，但拉取 `SQLiteCustom/src` 被受限环境拒绝；没有改动依赖或产品代码：
  - `swift test --disable-sandbox --package-path <临时目录>/step1/Packages/SynoraCore --scratch-path <临时目录>/step1/build --filter SynoraDomainTests -Xswiftc -warnings-as-errors`
- 随后在同一命令上补齐 GRDB 与 `SQLiteLib` 的本地 `insteadOf` 映射，并重跑；最终产物保存在 `/private/tmp/synora-p2-history-logs-final.ECjR2N/step1/result.log`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain focused（仅 GRDB 映射） | 1 | 失败：`SQLiteCustom/src` 的 HTTPS 子模块传输被环境拒绝，属测试环境问题 |
| Domain focused（GRDB + SQLiteLib 本地映射） | 1 | `PASS`，6 个测试、0 失败 |
| `git show --check c984547` | 1 | `PASS`，历史实现提交无空白错误 |

## 5. 用户如何进行手工测试

本轮没有把领域层接入应用界面，因此没有新增 UI 手工入口。可在 SwiftPM 试验代码中创建带列表父块和子块的 `BlockDocument`，检查排序、编码/解码和未知字段保留；再传入缺失父节点或自环，确认构造直接抛出 `BlockTreeError`。

## 6. 风险预估与规避

- **树结构破坏（高）**：构造和每次变更后统一调用 `validate()`，拒绝重复 ID、跨记录父节点、非法子块和环。
- **旧数据无法读取（中）**：旧 payload 的块类型、order key、revision 和新增字段使用 paragraph/稀疏位置等兼容默认值；未知字段作为 `JSONValue` 保留。
- **排序间隔耗尽（中）**：初始 order key 以 1024 递增，当前步骤没有声称完成重排；后续块操作步骤负责处理无间隔插入。
- **范围漂移（高）**：未修改 Store、EditorKit、App 或 `PLAN.md`，也未把本步骤当作 P2 阶段通过。

## 7. 下轮建议

按既定依赖先记录并验证 P2-09A 的真实 SQLite 事务基础，再进入 P2-02 的连续文本编辑核心；继续保持每步单独日志和单独提交。

## 8. 本轮任务大白话讲解

这一步先把记录里的内容从“一段字符串”变成有身份、有类型、有父子关系的内容树：标题、列表和任务能够被保存为明确的块，列表子项不会和别的记录串在一起，旧数据仍能读回来。用户暂时看不到新的界面，但后面的编辑器和保存层都将使用这份稳定的数据合同，而不是各自猜字段。
# 2026-09-07 16:00 · P 2 · 第 0 步，对应开发计划中的 0 步 · 固定 P2 工作基线

## 1. 本轮任务列表

- 重新核对 `HEAD`、索引和工作树，确认另一会话的既有差异不被本轮修改、暂存或提交。
- 核查 P0 编辑/存储直接依赖、现有 SwiftPM focused 测试和 P1 产品单测入口。
- 使用独立的临时 SwiftPM scratch、缓存、DerivedData、测试库和日志目录运行基线检查。
- 记录依赖网络失败、Xcode 缓存权限失败与本地镜像重跑结果，不把环境失败改写成代码成功。
- 建立本阶段唯一开发日志文件和当前任务证据边界；本轮不修改 `PLAN.md`、`SPEC.md` 或产品代码。

## 2. 本轮任务完成度

`100%`：P2 第 0 步的工作树核对、直接依赖核查、focused 基线运行、问题分类和日志建立均已完成。

当前基线事实：

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 当前分支与远端 | `main`，比 `origin/main` 超前 10 个提交 | `git status --short --branch` |
| 另一会话差异 | 7 个应用文件保持未暂存、未修改 | `git diff --name-only` |
| Domain focused | `9/9 PASS` | `/private/tmp/synora-p2-baseline-domain-local.XQcql5/result.log` |
| StoreProbe focused | `3/3 PASS` | `/private/tmp/synora-p2-baseline-store-local.9nxAWe/result.log` |
| P1 产品单测 | `UNRESOLVED`，环境退出码 `74` | Xcode 无法写入全局 SwiftPM manifest diagnostics |
| 当前阶段状态 | P2 仍按共享计划保持 `NOT_STARTED` | 未改动 `docs/PLAN.md`、`docs/SPEC.md` |

已有提交中的 P2 代码仍按计划附带的当前状态处理，不在本轮把历史提交直接宣称为阶段验收；下一轮从当前未接受的 P2-03 继续。

## 3. 测试方式

- 初次使用独立 scratch 运行 Domain 和 StoreProbe focused 测试时，SwiftPM 解析 GRDB 依赖因网络/DNS 失败；没有修改依赖声明。
- 随后从现有本地 checkout 建立临时 bare mirror，通过临时 `GIT_CONFIG_GLOBAL` 的 `insteadOf` 映射和 `--disable-sandbox` 重跑：
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraDomainTests -Xswiftc -warnings-as-errors`
  - `swift test --disable-sandbox --package-path Packages/SynoraCore --scratch-path <临时目录>/build --filter SynoraStoreProbeTests -Xswiftc -warnings-as-errors`
- 运行 P1 产品单测：`./script/p1.sh unit`，并分别使用独立 `SYNORA_ARTIFACT_DIR`、`SYNORA_DERIVED_DATA`、Clang/SwiftPM 临时缓存和本地 GRDB/SQLite mirror 重跑。
- 运行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Domain focused（首次远程解析） | 1 | 失败：GRDB 依赖网络/DNS，环境问题 |
| Domain focused（本地 mirror） | 1 | `PASS`，9 个测试、0 失败 |
| StoreProbe focused（首次远程解析） | 1 | 失败：GRDB 依赖网络/DNS，环境问题 |
| StoreProbe focused（本地 mirror） | 1 | `PASS`，3 个测试、0 失败 |
| P1 `./script/p1.sh unit`（首次） | 1 | 失败：SwiftPM 依赖解析环境问题 |
| P1 `./script/p1.sh unit`（本地 mirror/全临时缓存） | 1 | 失败：Xcode 仍尝试写入 `/Users/qilong.lu/Library/Caches/org.swift.swiftpm/manifests/ManifestLoading/synoracore.dia`，退出码 `74` |
| `git diff --check` | 1 | `PASS` |

本轮没有遇到产品断言失败；P1 单测退出码 74 归类为当前受限环境的权限/缓存问题，不能计为 P1 通过，也不阻塞独立的 P2 代码工作。

## 5. 用户如何进行手工测试

本轮只有基线和日志，没有产品行为改动，不需要 UI 手工验收。需要复核边界时，可运行 `git status --short`，确认 7 个既有应用文件仍未被本轮暂存或修改。

## 6. 风险预估与规避

- **并发改动覆盖（高）**：提交前再次核对 `HEAD`、索引和工作树；本提交只暂存本日志文件，不使用 `git add .`。
- **依赖解析/缓存权限（中）**：只使用 `/private/tmp` 下的临时 mirror、scratch 和缓存；不改 `Package.swift`、全局缓存或代理配置。
- **测试证据误读（高）**：保留首次失败和重跑失败的原始结论；不把 P1 退出码 74 解释为产品失败或成功。
- **阶段状态漂移（高）**：本轮不编辑共享 `PLAN.md`/`SPEC.md`；P2 不提前标记 `PASS`，也不启动后续阶段。

## 7. 下轮建议

按执行计划进入当前未接受的 **P2-03 基础块**，先读取实际调用链，再只实现该步骤范围，运行 focused 领域/编辑器检查、`git diff --check`，更新本日志顶部后单独提交。若共享文件出现新的并发差异，先暂停受影响文件。

## 8. 本轮任务大白话讲解

这一步是在正式做编辑器前把“地面”钉住：先确认哪一个提交是起点、哪些文件属于另一会话、现有底层测试能不能在隔离目录运行。Domain 和 StoreProbe 的小测试已经能通过，说明块模型和存储探针的当前代码可被继续使用；P1 单测卡在 Xcode 无法写全局缓存，所以这里只记录真实阻塞，不假装测试成功。下一步才处理用户能看到的段落、标题、列表等基础块。
# 2026-09-08 01:15 · P 2 · 第 19 步，对应开发计划中的 12 步 · 原生编辑器焦点与输入闭环

## 1. 本轮任务列表

- 用真实 macOS 窗口复现编辑器可见但无法输入的断点，确认事件没有进入内部 `NSTextView`。
- 修正 `SynoraTextView` 的可访问性归属和首响应者转发，让用户点击正文后由内部 TextKit 视图接收键盘输入。
- 用原生 CUA 和 XCUITest 复核查找替换、正文输入及编辑工具入口，并保留当前 P2 未闭合的性能、VoiceOver 与故障矩阵门槛。

## 2. 本轮任务完成度

断点根因是外层 `NSView` 与内部 `NSTextView` 共享 `editor-body` 可访问性身份，点击命中外层视图后没有把首响应者交给 TextKit。现在内部视图独占 `editor-body` 文本区域身份，外层降为容器并显式转发首响应者；真实窗口中点击正文后输入 ` edited` 已出现在 AX value，查找替换也保留 `Local-ready`。未改变 EditorSession、revision 或保存协议。

## 3. 测试方式

- 使用独立 DerivedData 构建 Debug App。
- 在临时 fixture 窗口中用 CUA 点击内部 `editor-body` 并输入文本，读取前后 AX value。
- 运行 `testEditorWorkflowExposesActionsAndEditsBody`，验证 All Notes、正文入口、Block/Format/Undo/Redo/Find/Export、Replace All 和正文输入动作。
- 提交前执行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| Xcode Debug App build（焦点修复） | 1 | `PASS`，`** BUILD SUCCEEDED **`；产物 `/private/tmp/synora-p2-focus-fix/Build/Products/Debug/SynoraWiki.app` |
| CUA 原生正文输入 | 1 | `PASS`，`editor-body` AX value 从 `Small constraints...` 变为追加 ` edited` |
| XCUITest 编辑器旅程 | 1 | `PASS`，1 个测试、0 失败；xcresult `/tmp/synora-p2-ui-editor-pass/Logs/Test/Test-SynoraWiki-2026.09.08_01-14-16-+0800.xcresult` |
| `git diff --check` | 1 | `PASS` |

## 5. 未闭合证据

XCUITest 能稳定验证 Replace All 的正文结果；正文键盘输入的最终内容以 CUA 原生 AX 复核为准，仍需纳入后续屏幕性能与真实 VoiceOver 证据归档。P2 当前继续为 `CHANGES_REQUIRED`。
# 2026-09-08 01:58 · P 2 · 第 20 步 · 大型媒体宿主的输入重绘隔离

## 1. 本轮任务列表

- 用当前 Debug App 打开真实 100k UTF-16 字符、200 个媒体资产的隔离库，并用 Points of Interest attach 采集输入到绘制的屏幕 signpost。
- 根据 trace 中反复出现的 `RecordMediaStack.mediaAsset`、`LocalMediaPlayerView.updateNSView` 和 SwiftUI graph update，隔离正文输入对媒体宿主的无效重建。
- 保留可观察的媒体 block/asset 变化作为 Equatable 输入，确保替换、删除、说明和布局操作仍然能刷新媒体界面。

## 2. 本轮任务完成度

首轮真实窗口 trace 取得了 `keystrokeToPaint` 完整样本约 `1,065 ms`，time-profile 在输入后持续重建媒体视图；这不是模型耗时。`RecordMediaStack` 现在由可比较的稳定配置包裹，正文字符变化只要媒体 block 和资产集合没有变化就跳过整个媒体子树更新，后续将用同一隔离库和同一 Instruments 模板复测。

## 3. 测试方式

- 修改范围仅为 `App/Records/RecordEditorView.swift` 和本开发日志。
- 使用本地依赖与独立 DerivedData 重建 Debug App，随后复用同一大型隔离库做 attach 复测。
- 提交前执行 `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| 修改前真实 `keystrokeToPaint` trace | 1 | `FAIL`，完整样本约 `1,065 ms`；原始 trace `/private/tmp/synora-p2-large-input-r2.trace` |
| time-profile 根因定位 | 1 | `PASS`，输入期间出现 `RecordMediaStack.mediaAsset`、`RecordMediaStack.previewContent` 和 `LocalMediaPlayerView.updateNSView` 热点 |
| 稳定媒体配置编译检查 | 待本轮完成 | `UNVERIFIED` |
| `git diff --check` | 待本轮完成 | `UNVERIFIED` |

## 5. 未闭合证据

本轮只完成性能根因的最小代码修复；修复后的真实输入 P95/P99、首屏和滚动样本、真实 IME/VoiceOver、导出 UI 及文件系统故障矩阵仍未通过，P2 继续为 `CHANGES_REQUIRED`。
# 2026-09-08 03:08 · P 2 · 第 22 步，对应开发计划中的 4 步 · 编辑状态与持久化根因修复

## 1. 本轮任务列表

- 让应用正文适配器递归处理嵌套块，统一编辑、媒体操作和重开文本。
- 让异步媒体文档接管现有 `EditorSession`，保留撤销栈。
- 让已提交但被取消的保存任务回写 revision；较新的本地草稿继续保存。

## 2. 本轮任务完成度

已完成并通过针对性验证；未改变存储协议。性能候选的视图重建修复仍待真实窗口验证。

## 3. 测试方式

- `./script/p2.sh unit`，固定 P2 SwiftPM 缓存，运行 1 次。
- `./script/p1.sh unit`，固定 P2 Xcode DerivedData，运行 1 次。
- `git diff --check`。

## 4. 测试结果

| 检查 | 运行次数 | 结果 |
| --- | ---: | --- |
| SynoraEditorKit、Domain、Store、Assets、Export | 1 | `PASS`，59 个测试、0 失败 |
| SynoraWikiTests | 1 | `PASS`，16 个测试、0 失败 |
| `git diff --check` | 1 | `PASS` |

## 5. 用户如何进行手工测试

在真实记录中编辑嵌套块，执行媒体说明或替换后立即输入正文；等待保存完成并重启应用，确认正文、媒体修改和撤销顺序保留。

## 6. 风险预估与规避

保存仍以本地 revision 冲突保护为边界；外部并发修改继续显示冲突，不静默覆盖。异步媒体预览尚未作为屏幕性能证据验收。

## 7. 下轮建议

补齐缩进、移出、callout 样式和表格操作入口，随后验证媒体与导出旅程。

## 8. 本轮任务大白话讲解

编辑器以前有些路径绕过了自己的撤销记录，嵌套块也可能被压平成顶层文字。现在统一走同一份编辑会话，后台保存完成后不会把新输入覆盖掉。
