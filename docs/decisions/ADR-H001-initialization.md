# ADR-H001 工程初始化与阶段顺序

状态：ACCEPTED；日期：2026-09-05；来源：本任务用户明确指令。

- `main` 跟踪新建远程 origin，保留初始 LICENSE/历史。首版后再保护 main、创建 dev；现在在 main 工作。
- P4 改为 AI Runtime/BYOK/行内 AI，P5 改为 LLM Wiki。新 P4 在 P3 数据之上交付通用提案提交链；新 P5 扩展 Wiki 校验，避免仅换编号留下循环依赖。全部原任务保留，ID 同步映射。
- 原型是本地设计参考，不是真实实现；代码和图像移至 `local-reference/high-fidelity/`，不跟踪、不上传。目录缺失时仍可读规范，但无法做视觉参考验收，应明确报告。
- 原 QA 不自动继承。授权按实际产品与 macOS 规则校准尺寸、可访问性和验证方法，调整以实测事实为依据。
- `.agents/` 与 `.harness/` 只保留为本机工具/状态并由 Git 忽略；持久规则、决策和未决项落在 AGENTS 与 `docs/`。
- 本轮只做工程治理，P0–P9 均未启动。README 留空。

原始文档采用 ZIP 原样封存而非另建活动副本；SHA-256 清单可逐文件核对。活动文档的阅读与修改责任只由根 AGENTS 定义。撤回本轮文档调整时从档案提取到临时目录比较，不覆盖后续用户修改。
